# FULL MASTER → FEATURE LINE-BY-LINE ANNOTATION — PART 4/4

**Frozen compare:** `06390e5357a07f80a8089ac28fc16c75461a48a6` → `3c17c359fd8d0a72e9ea2cdb3d79630a9e93324b`  
**Purpose:** every raw added/deleted line from the frozen implementation diff receives an individual explanation.  
**Note:** these files intentionally freeze the implementation before the annotation documents themselves were added, avoiding recursive self-diff.


## `eval/phase4_pro_rsi.py` — modified, +648/-79

**Shared rationale:** answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof.


### `@@ -28,6 +28,7 @@`

- **ADD** `from core.training_memory import process_memory_bytes` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -28,6 +28,7 @@`.

### `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`

- **ADD** `def _clip_verifier_feedback(text: str, limit: int = 600) -> str:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `"""Keep deterministic verifier feedback short and free of hidden-answer dumps."""` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `clean = " ".join(str(text or "").split())` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `return clean[: max(1, int(limit))]` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `blank-line formatting change within this audited hunk` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `blank-line formatting change within this audited hunk` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `def _sandbox_failure_feedback(result: Any, prefix: str) -> str:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `"""Return useful structural/runtime feedback without exposing assertion payloads."""` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `if bool(getattr(result, "passed", False)):` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `return ""` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `blank-line formatting change within this audited hunk` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `raw = str(getattr(result, "error", "") or "").strip()` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `if not raw:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `return f"{prefix}: deterministic verifier rejected the selected candidate."` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `blank-line formatting change within this audited hunk` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `# Hidden tests can embed expected values in assertion text. Preserve only safe` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `# failure class information for assertion/test failures; syntax/runtime errors` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `# from the candidate remain useful for self-correction.` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `lowered = raw.lower()` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `if "assertionerror" in lowered or "assert " in lowered:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `return f"{prefix}: deterministic assertion/test failed; inspect the candidate logic and edge cases."` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `blank-line formatting change within this audited hunk` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `last = raw.splitlines()[-1].strip()` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `if not last:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `return f"{prefix}: deterministic verifier rejected the selected candidate."` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `return _clip_verifier_feedback(f"{prefix}: {last}")` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `blank-line formatting change within this audited hunk` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `blank-line formatting change within this audited hunk` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `def _answer_blind_failure_feedback(` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `self,` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `split: str,` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `item: Dict[str, Any],` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `candidate: str,` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `) -> str:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `"""Derive round-2 correction signal only from prompt-visible/deterministic verifiers."""` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `try:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `kind = str(item.get("_real_kind") or "").strip().lower()` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `code = clean_output(candidate)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `blank-line formatting change within this audited hunk` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `if kind == "humaneval":` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `result = self.engine.sandbox.execute_python_code(` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `str(item["prompt"]) + "\n" + code,` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `str(item["test"]),` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `return _sandbox_failure_feedback(result, "Python verifier")` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `blank-line formatting change within this audited hunk` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `if kind == "livecodebench":` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `result = self.engine.sandbox.execute_python_code(` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `"SOLUTION = " + repr(code),` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `str(item["test"]),` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `return _sandbox_failure_feedback(result, "Python verifier")` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `blank-line formatting change within this audited hunk` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `if kind == "code_repair":` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `result = self.engine.sandbox.execute_python_code(` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `code,` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `str(item["test"]),` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `return _sandbox_failure_feedback(result, "Repair verifier")` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `blank-line formatting change within this audited hunk` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `if "HumanEval" in split:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `result = self.engine.sandbox.execute_python_code(` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `str(item["prompt"]) + "\n" + code,` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `str(item["test"]),` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `return _sandbox_failure_feedback(result, "Python verifier")` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `blank-line formatting change within this audited hunk` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `if "LiveCodeBench" in split:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `result = self.engine.sandbox.execute_python_code(` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `code,` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `str(item["test"]),` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `return _sandbox_failure_feedback(result, "Python verifier")` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `blank-line formatting change within this audited hunk` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `if "DeepSWE" in split:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `result = self.engine.sandbox.verify_git_diff_patch(` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `item["repo_files"],` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `code,` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `item["test_cmd"],` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `# Patch/test output can include hidden expected values, so keep this` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `# intentionally structural rather than echoing raw test output.` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `if not bool(getattr(result, "passed", False)):` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `return (` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `"Patch verifier: selected patch failed deterministic verification; "` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `"re-check the edited lines, imports, and test-sensitive edge cases."` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `return ""` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `blank-line formatting change within this audited hunk` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `if "TensorGraphDSL" in split:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `value = self.engine.sandbox.evaluate_dsl_expression(item["dsl_expr"])` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `if value is None:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `return (` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `"DSL verifier: expression could not be evaluated; re-apply the "` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `"literal fold/scale/fuse operators exactly once."` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `cleaned = clean_output(candidate).replace(" ", "")` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `if str(value).replace(" ", "") not in cleaned:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `return (` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `"DSL verifier: selected result was inconsistent with the prompt-visible "` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `"DSL semantics; re-apply fold/scale/fuse exactly once."` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `return ""` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `blank-line formatting change within this audited hunk` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `if "BFCL" in split:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `requested_name, requested_args = _prompt_requested_bfcl(item)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `value = _parse_json_object(candidate)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `if not isinstance(value, dict):` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `return "Tool-call verifier: return exactly one valid JSON object."` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `if isinstance(value.get("function"), dict):` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `value = value["function"]` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `name = value.get("name") or value.get("tool")` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `args = value.get("arguments") or value.get("args")` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `if isinstance(args, str):` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `try:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `args = json.loads(args)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `except Exception:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `return "Tool-call verifier: arguments must be one valid JSON object."` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `if requested_name and name != requested_name:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `return "Tool-call verifier: selected tool name did not match the user request."` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `if requested_args is not None and args != requested_args:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `return "Tool-call verifier: selected arguments did not match the user request."` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `except Exception as exc:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `# Exception type is safe/useful; avoid serializing full hidden-test payloads.` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `return f"Deterministic verifier raised {type(exc).__name__}; revise the selected candidate."` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `blank-line formatting change within this audited hunk` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `return ""` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `blank-line formatting change within this audited hunk` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.
- **ADD** `blank-line formatting change within this audited hunk` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -299,6 +300,135 @@ def _candidate_passes_answer_blind(`.

### `@@ -527,9 +657,11 @@ def _run_rsi_self_improvement(self, splits, cache) -> int:`

- **ADD** `verifier_feedback = ""` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -527,9 +657,11 @@ def _run_rsi_self_improvement(self, splits, cache) -> int:`.
- **ADD** `_assert_same_model(self, model_identity, f"RSI round {round_idx}")` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -527,9 +657,11 @@ def _run_rsi_self_improvement(self, splits, cache) -> int:`.

### `@@ -542,7 +674,14 @@ def _run_rsi_self_improvement(self, splits, cache) -> int:`

- **REMOVE** `+ "\n\nCritique your own attempt, identify what may be wrong without access to any hidden answer, "` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -542,7 +674,14 @@ def _run_rsi_self_improvement(self, splits, cache) -> int:`.
- **ADD** `)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -542,7 +674,14 @@ def _run_rsi_self_improvement(self, splits, cache) -> int:`.
- **ADD** `if verifier_feedback:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -542,7 +674,14 @@ def _run_rsi_self_improvement(self, splits, cache) -> int:`.
- **ADD** `rsi_user += (` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -542,7 +674,14 @@ def _run_rsi_self_improvement(self, splits, cache) -> int:`.
- **ADD** `"\n\nAnswer-blind deterministic verifier feedback from that attempt:\n"` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -542,7 +674,14 @@ def _run_rsi_self_improvement(self, splits, cache) -> int:`.
- **ADD** `+ verifier_feedback` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -542,7 +674,14 @@ def _run_rsi_self_improvement(self, splits, cache) -> int:`.
- **ADD** `)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -542,7 +674,14 @@ def _run_rsi_self_improvement(self, splits, cache) -> int:`.
- **ADD** `rsi_user += (` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -542,7 +674,14 @@ def _run_rsi_self_improvement(self, splits, cache) -> int:`.
- **ADD** `"\n\nCritique your own attempt, identify what may be wrong without access to any hidden answer, "` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -542,7 +674,14 @@ def _run_rsi_self_improvement(self, splits, cache) -> int:`.

### `@@ -561,11 +700,24 @@ def _run_rsi_self_improvement(self, splits, cache) -> int:`

- **ADD** `# Only the selected candidate is needed from here on. Release the other` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -561,11 +700,24 @@ def _run_rsi_self_improvement(self, splits, cache) -> int:`.
- **ADD** `# potentially long branch strings before verification / recursive round 2.` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -561,11 +700,24 @@ def _run_rsi_self_improvement(self, splits, cache) -> int:`.
- **ADD** `branches.clear()` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -561,11 +700,24 @@ def _run_rsi_self_improvement(self, splits, cache) -> int:`.
- **ADD** `branches = None` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -561,11 +700,24 @@ def _run_rsi_self_improvement(self, splits, cache) -> int:`.
- **ADD** `gc.collect(1)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -561,11 +700,24 @@ def _run_rsi_self_improvement(self, splits, cache) -> int:`.
- **ADD** `blank-line formatting change within this audited hunk` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -561,11 +700,24 @@ def _run_rsi_self_improvement(self, splits, cache) -> int:`.
- **ADD** `if not passed and round_idx == 1:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -561,11 +700,24 @@ def _run_rsi_self_improvement(self, splits, cache) -> int:`.
- **ADD** `verifier_feedback = _answer_blind_failure_feedback(` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -561,11 +700,24 @@ def _run_rsi_self_improvement(self, splits, cache) -> int:`.
- **ADD** `self,` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -561,11 +700,24 @@ def _run_rsi_self_improvement(self, splits, cache) -> int:`.
- **ADD** `split_name,` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -561,11 +700,24 @@ def _run_rsi_self_improvement(self, splits, cache) -> int:`.
- **ADD** `item,` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -561,11 +700,24 @@ def _run_rsi_self_improvement(self, splits, cache) -> int:`.
- **ADD** `candidate,` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -561,11 +700,24 @@ def _run_rsi_self_improvement(self, splits, cache) -> int:`.
- **ADD** `)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -561,11 +700,24 @@ def _run_rsi_self_improvement(self, splits, cache) -> int:`.

### `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`

- **ADD** `def _phase3_fmt_eta(seconds: Optional[float]) -> str:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `if seconds is None or seconds < 0:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `return "calculating"` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `if seconds <= 0:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `return "0s"` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `seconds = int(seconds)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `hours, rem = divmod(seconds, 3600)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `minutes, secs = divmod(rem, 60)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `if hours:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `return f"{hours}h {minutes:02d}m"` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `if minutes:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `return f"{minutes}m {secs:02d}s"` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `return f"{secs}s"` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `blank-line formatting change within this audited hunk` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `blank-line formatting change within this audited hunk` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `def _phase3_stack(model):` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `"""Resolve the loaded causal language stack without replacing/reloading it."""` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `text_model = getattr(model, "language_model", None) or model` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `inner = getattr(text_model, "model", None) or text_model` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `blank-line formatting change within this audited hunk` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `layers = getattr(inner, "pipeline_layers", None)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `if layers is None:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `layers = getattr(inner, "layers", None)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `if layers is None:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `layers = getattr(model, "layers", None)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `blank-line formatting change within this audited hunk` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `layers = list(layers or [])` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `embed_tokens = getattr(inner, "embed_tokens", None)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `norm = getattr(inner, "norm", None)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `if not layers or embed_tokens is None or norm is None:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `raise RuntimeError(` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `"Phase 3 bounded training could not resolve the loaded transformer stack"` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `return text_model, inner, layers, embed_tokens, norm` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `blank-line formatting change within this audited hunk` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `blank-line formatting change within this audited hunk` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `def _phase3_window_value_and_grad(self, inp, target_start: int, target_end: int):` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `"""Backprop one small completion window one decoder layer at a time."""` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `model = self.engine.model` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `text_model, _inner, layers, embed_tokens, norm = _phase3_stack(model)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `blank-line formatting change within this audited hunk` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `module_paths = {}` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `try:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `module_paths = {id(module): name for name, module in model.named_modules()}` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `except Exception:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `pass` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `blank-line formatting change within this audited hunk` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `hidden = embed_tokens(inp)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `mx.eval(hidden)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `hidden = mx.stop_gradient(hidden)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `boundaries = [hidden]` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `blank-line formatting change within this audited hunk` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `for layer in layers:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `mask = None if bool(getattr(layer, "is_linear", False)) else "causal"` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `hidden = layer(hidden, mask=mask, cache=None)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `mx.eval(hidden)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `hidden = mx.stop_gradient(hidden)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `boundaries.append(hidden)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `blank-line formatting change within this audited hunk` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `final_hidden = boundaries[-1]` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `targets = inp[:, target_start + 1 : target_end + 1]` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `blank-line formatting change within this audited hunk` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `def head_loss(local_hidden):` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `selected = local_hidden[:, target_start:target_end, :]` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `z = norm(selected)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `if hasattr(text_model, "lm_head"):` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `logits = text_model.lm_head(z)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `elif hasattr(embed_tokens, "as_linear"):` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `logits = embed_tokens.as_linear(z)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `else:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `raise RuntimeError("Phase 3 could not resolve LM head")` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `return mx.mean(` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `nn.losses.cross_entropy(` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `logits.astype(mx.float32),` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `targets,` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `blank-line formatting change within this audited hunk` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `loss, cotangent = mx.value_and_grad(head_loss)(final_hidden)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `mx.eval(loss, cotangent)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `cotangent = mx.stop_gradient(cotangent)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `blank-line formatting change within this audited hunk` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `flat_grads: Dict[str, Any] = {}` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `blank-line formatting change within this audited hunk` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `for layer_idx in range(len(layers) - 1, -1, -1):` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `layer = layers[layer_idx]` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `h_in = boundaries[layer_idx]` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `incoming = mx.stop_gradient(cotangent)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `mask = None if bool(getattr(layer, "is_linear", False)) else "causal"` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `params = layer.trainable_parameters()` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `local_param_items = list(mlx.utils.tree_flatten(params))` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `blank-line formatting change within this audited hunk` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `def forward_layer(local_params, local_hidden):` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `layer.update(local_params)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `return layer(local_hidden, mask=mask, cache=None)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `blank-line formatting change within this audited hunk` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `checkpointed = mx.checkpoint(forward_layer)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `blank-line formatting change within this audited hunk` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `if local_param_items:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `def scalar(local_params, local_hidden):` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `out = checkpointed(local_params, local_hidden)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `return mx.sum(` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `out.astype(mx.float32)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `* incoming.astype(mx.float32)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `blank-line formatting change within this audited hunk` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `_, pair = mx.value_and_grad(` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `scalar,` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `argnums=[0, 1],` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `)(params, h_in)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `param_grads, input_grad = pair` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `flat_local = list(mlx.utils.tree_flatten(param_grads))` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `mx.eval(input_grad, *[grad for _, grad in flat_local])` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `blank-line formatting change within this audited hunk` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `path = module_paths.get(id(layer), "")` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `for local_key, grad in flat_local:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `key = f"{path}.{local_key}" if path else str(local_key)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `flat_grads[key] = mx.stop_gradient(grad)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `else:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `def scalar_hidden(local_hidden):` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `out = layer(local_hidden, mask=mask, cache=None)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `return mx.sum(` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `out.astype(mx.float32)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `* incoming.astype(mx.float32)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `blank-line formatting change within this audited hunk` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `input_grad = mx.grad(scalar_hidden)(h_in)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `mx.eval(input_grad)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `blank-line formatting change within this audited hunk` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `cotangent = mx.stop_gradient(input_grad)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `boundaries[layer_idx + 1] = None` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `blank-line formatting change within this audited hunk` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `try:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `mx.clear_cache()` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `except Exception:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `pass` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `blank-line formatting change within this audited hunk` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `expected = dict(mlx.utils.tree_flatten(model.trainable_parameters()))` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `missing = set(expected) - set(flat_grads)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `extra = set(flat_grads) - set(expected)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `if missing or extra:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `raise RuntimeError(` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `"Phase 3 bounded gradient mismatch: "` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `f"missing={sorted(missing)[:6]} extra={sorted(extra)[:6]}"` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `blank-line formatting change within this audited hunk` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `grads = mlx.utils.tree_unflatten(` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `[(key, flat_grads[key]) for key in expected]` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `return loss, grads` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `blank-line formatting change within this audited hunk` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `blank-line formatting change within this audited hunk` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `def _phase3_bounded_gradients(` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `self,` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `ids: List[int],` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `completion_loss_start: int,` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `memory_index: int,` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `memory_total: int,` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `global_target_offset: int,` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `global_total_targets: int,` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `global_started: float,` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `):` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `"""Train the completion through 64-token windows without retaining a full-model graph."""` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `max_row_tokens = 16_384` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `original_len = len(ids)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `selected_start = max(0, original_len - max_row_tokens)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `selected = list(ids[selected_start:])` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `if len(selected) < 2:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `raise RuntimeError("Phase 3 training row has fewer than two tokens")` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `blank-line formatting change within this audited hunk` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `full = mx.array([selected])` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `total_targets = len(selected) - 1` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `first_target = max(0, int(completion_loss_start) - selected_start)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `first_target = min(first_target, total_targets)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `if first_target >= total_targets:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `raise RuntimeError("Phase 3 completion tokens fell outside the bounded training row")` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `blank-line formatting change within this audited hunk` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `window_tokens = 64` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `targets_per_window = 32` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `aggregate: Dict[str, Any] = {}` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `weighted_loss = 0.0` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `trained_targets = 0` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `target_global = first_target` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `completion_targets = total_targets - first_target` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `while target_global < total_targets:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `target_end = min(total_targets, target_global + targets_per_window)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `window_end = target_end + 1` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `window_start = max(0, window_end - window_tokens)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `blank-line formatting change within this audited hunk` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `local = full[:, window_start:window_end]` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `local_start = target_global - window_start` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `local_end = target_end - window_start` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `count = target_end - target_global` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `blank-line formatting change within this audited hunk` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `loss, grads = _phase3_window_value_and_grad(` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `self,` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `local,` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `local_start,` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `local_end,` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `blank-line formatting change within this audited hunk` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `flat = dict(mlx.utils.tree_flatten(grads))` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `mx.eval(loss, *flat.values())` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `blank-line formatting change within this audited hunk` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `for key, grad in flat.items():` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `weighted = mx.stop_gradient(grad) * float(count)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `mx.eval(weighted)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `if key in aggregate:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `value = aggregate[key] + weighted` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `mx.eval(value)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `aggregate[key] = mx.stop_gradient(value)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `else:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `aggregate[key] = mx.stop_gradient(weighted)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `blank-line formatting change within this audited hunk` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `weighted_loss += float(loss.item()) * count` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `trained_targets += count` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `target_global = target_end` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `blank-line formatting change within this audited hunk` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `global_trained = int(global_target_offset) + int(trained_targets)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `elapsed = max(0.001, time.monotonic() - float(global_started))` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `speed = global_trained / elapsed` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `pct = 100.0 * global_trained / max(1, int(global_total_targets))` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `remaining_estimate = max(0, int(global_total_targets) - global_trained)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `total_eta = remaining_estimate / speed if speed > 0 else None` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `blank-line formatting change within this audited hunk` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `try:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `ram_mb = process_memory_bytes() / (1024 ** 2)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `except Exception:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `ram_mb = 0.0` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `blank-line formatting change within this audited hunk` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `print(` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `f"Phase 3: {global_trained}/{global_total_targets}"` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `f" | {pct:.1f}%"` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `f" | {speed:.2f} tgt/s"` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `f" | Total ETA {_phase3_fmt_eta(total_eta)}"` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `f" | RAM {ram_mb:.0f} MB",` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `flush=True,` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `blank-line formatting change within this audited hunk` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `loss = None` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `grads = None` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `flat = None` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `local = None` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `gc.collect(1)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `try:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `mx.clear_cache()` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `except Exception:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `pass` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `blank-line formatting change within this audited hunk` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `inv = 1.0 / float(max(1, trained_targets))` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `for key in list(aggregate):` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `value = aggregate[key] * inv` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `mx.eval(value)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `aggregate[key] = mx.stop_gradient(value)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `blank-line formatting change within this audited hunk` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `# Preserve the newer transaction wrapper's real Fisher/EWC protection. The` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `# wrapper supplies only small LoRA snapshots/fisher tensors; no full graph is kept.` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `ewc = getattr(self, "_phase3_ewc_context", None)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `if isinstance(ewc, dict) and float(ewc.get("lambda", 0.0) or 0.0) > 0.0:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `fisher = ewc.get("fisher") or {}` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `reference = ewc.get("reference") or {}` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `current = dict(` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `mlx.utils.tree_flatten(` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `self.engine.model.trainable_parameters()` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `lam = float(ewc.get("lambda", 0.0) or 0.0)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `for key in list(aggregate):` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `if key not in fisher or key not in reference or key not in current:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `continue` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `value = (` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `aggregate[key]` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `+ lam * fisher[key] * (current[key] - reference[key])` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `mx.eval(value)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `aggregate[key] = mx.stop_gradient(value)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `blank-line formatting change within this audited hunk` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `grads = mlx.utils.tree_unflatten(list(aggregate.items()))` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `return (` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `mx.array(weighted_loss * inv, dtype=mx.float32),` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `grads,` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `int(trained_targets),` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `blank-line formatting change within this audited hunk` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `blank-line formatting change within this audited hunk` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **REMOVE** `"""Train the exact already-loaded model in-place on Learn + RSI traces."""` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.
- **ADD** `"""Train the exact already-loaded model in-place with bounded completion-only graphs."""` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -710,8 +862,293 @@ def _restore_rsi_adapter(self) -> bool:`.

### `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`

- **ADD** `trainable = dict(` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `mlx.utils.tree_flatten(` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `self.engine.model.trainable_parameters()` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `if not trainable:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `_pro_backend(self).inject_lora_adapters(r=8)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `trainable = dict(` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `mlx.utils.tree_flatten(` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `self.engine.model.trainable_parameters()` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `if not trainable:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `raise RuntimeError("Phase 3 has no trainable LoRA parameters")` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `blank-line formatting change within this audited hunk` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `bad = [` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `key for key in trainable` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `if key.rsplit(".", 1)[-1] not in {"lora_a", "lora_b"}` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `]` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `if bad:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `raise RuntimeError(` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `"Phase 3 refuses non-LoRA trainables: "` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `+ ", ".join(bad[:6])` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `blank-line formatting change within this audited hunk` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `# Real trainable-delta proof for the integrity layer. Snapshot only LoRA` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `# trainables, never the frozen foundation weights.` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `initial_trainable = {}` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `for key, value in trainable.items():` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `try:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `initial_trainable[key] = mx.copy(value)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `except Exception:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `initial_trainable[key] = value + mx.zeros_like(value)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `if initial_trainable:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `mx.eval(*initial_trainable.values())` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `blank-line formatting change within this audited hunk` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `was_training = bool(getattr(self.engine.model, "training", False))` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `for memory in memories:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `# Preserve the original Phase-3 weight-update contract. RSI memories are` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `# normalized by the fetcher into the same prompt+completion shape, using only` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `# a fixed generic cue plus the model's own self-generated trace.` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `text = (` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `f"<|im_start|>user\n{memory['prompt']}<|im_end|>\n"` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `f"<|im_start|>assistant\n{memory['completion']}<|im_end|>"` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `ids = self.engine.tokenizer.encode(text)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `if len(ids) <= 1:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `continue` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `try:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `self.engine.model.train()` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `blank-line formatting change within this audited hunk` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `# Precompute the exact completion-target count for the whole Phase-3 queue.` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `# Telemetry then stays cumulative across memory rows instead of resetting at` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `# 0/N every time a new Learn/RSI trace begins.` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `prepared_memories = []` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `phase3_total_targets = 0` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `max_row_tokens = 16_384` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `for memory in memories:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `prefix = (` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `f"<|im_start|>user\n{memory['prompt']}<|im_end|>\n"` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `f"<|im_start|>assistant\n"` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `text = prefix + str(memory["completion"]) + "<|im_end|>"` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `ids = self.engine.tokenizer.encode(text)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `prefix_ids = self.engine.tokenizer.encode(prefix)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `if len(ids) <= 1:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `continue` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `with METAL_STREAM_LOCK:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `inp = mx.array([ids[: min(len(ids), 256)]])` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `completion_loss_start = max(0, len(prefix_ids) - 1)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `selected_start = max(0, len(ids) - max_row_tokens)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `total_targets = min(len(ids), max_row_tokens) - 1` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `first_target = max(0, completion_loss_start - selected_start)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `first_target = min(first_target, total_targets)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `completion_targets = max(0, total_targets - first_target)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `if completion_targets <= 0:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `continue` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `def lossfn(model):` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `logits = model(inp)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `return mx.mean(` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `nn.losses.cross_entropy(` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `logits[:, :-1, :].astype(mx.float32),` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `inp[:, 1:],` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `# Keep only the row reference + scalar count from the pre-scan.` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `# Retaining every full token-id list here can consume substantial Python` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `# heap for long RSI traces and defeats the bounded-memory design.` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `prepared_memories.append((memory, completion_targets))` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `phase3_total_targets += completion_targets` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `ids = None` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `prefix_ids = None` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `loss, grads = nn.value_and_grad(self.engine.model, lossfn)(` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `self.engine.model` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `if phase3_total_targets <= 0:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `raise RuntimeError("Phase 3 found no trainable completion targets")` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `applied = False` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `try:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `flat, shapes = self.engine.ogp_projector.flatten_gradients(` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `dict(mlx.utils.tree_flatten(grads))` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `projected = self.engine.ogp_projector.project_gradient(flat)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `tree = self.engine.ogp_projector.unflatten_gradients(projected, shapes)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `opt.update(` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `self.engine.model,` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `mlx.utils.tree_unflatten(list(tree.items())),` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `phase3_started = time.monotonic()` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `phase3_completed_targets = 0` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `blank-line formatting change within this audited hunk` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `for memory_index, (memory, _row_targets) in enumerate(prepared_memories, 1):` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `prefix = (` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `f"<|im_start|>user\n{memory['prompt']}<|im_end|>\n"` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `f"<|im_start|>assistant\n"` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `text = prefix + str(memory["completion"]) + "<|im_end|>"` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `ids = self.engine.tokenizer.encode(text)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `prefix_ids = self.engine.tokenizer.encode(prefix)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `if len(ids) <= 1:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `raise RuntimeError("Phase 3 prepared row became untrainable during execution")` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `completion_loss_start = max(0, len(prefix_ids) - 1)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `blank-line formatting change within this audited hunk` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `with METAL_STREAM_LOCK:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `loss, grads, row_trained_targets = _phase3_bounded_gradients(` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `self,` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `ids,` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `completion_loss_start,` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `memory_index,` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `len(prepared_memories),` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `phase3_completed_targets,` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `phase3_total_targets,` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `phase3_started,` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `applied = True` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `except Exception:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `# RSI must actually train. If OGP projection is unsupported for this` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `# model/kernel, fall back to the same raw verified self-training gradient.` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `opt.update(self.engine.model, grads)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `fallback_updates += 1` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `applied = True` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `if applied:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `try:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `flat, shapes = self.engine.ogp_projector.flatten_gradients(` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `dict(mlx.utils.tree_flatten(grads))` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `projected = self.engine.ogp_projector.project_gradient(flat)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `tree = self.engine.ogp_projector.unflatten_gradients(` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `projected, shapes` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `opt.update(` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `self.engine.model,` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `mlx.utils.tree_unflatten(list(tree.items())),` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `except Exception:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `opt.update(self.engine.model, grads)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `fallback_updates += 1` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `blank-line formatting change within this audited hunk` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `phase3_completed_targets += int(row_trained_targets)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `_assert_same_model(self, model_identity, "Phase 3 consolidation")` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `blank-line formatting change within this audited hunk` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `# Important: the old implementation swapped a stale buffer B after training,` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `# which could restore pre-training LoRA weights. Refresh B from the trained` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `# model BEFORE the atomic swap so the learned state cannot be reverted.` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `if getattr(self.engine, "moe_manager", None) is not None:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `current = {` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `k: mx.array(v)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `for k, v in dict(` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `mlx.utils.tree_flatten(` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `self.engine.model.trainable_parameters()` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `loss = None` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `grads = None` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `flat = None` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `projected = None` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `tree = None` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `ids = None` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `prefix_ids = None` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `gc.collect(1)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `try:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `mx.clear_cache()` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `except Exception:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `pass` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `blank-line formatting change within this audited hunk` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `_assert_same_model(self, model_identity, "Phase 3 consolidation")` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `blank-line formatting change within this audited hunk` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `if getattr(self.engine, "moe_manager", None) is not None:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `current = {` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `key: mx.array(value)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `for key, value in dict(` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `mlx.utils.tree_flatten(` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `self.engine.model.trainable_parameters()` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `).items()` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `}` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `self.engine.moe_manager.adapters_buffer_b = current` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `self.engine.moe_manager.swap_buffers_atomic()` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `_assert_same_model(self, model_identity, "Phase 3 buffer swap")` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `blank-line formatting change within this audited hunk` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `current_trainable = dict(` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `mlx.utils.tree_flatten(` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `self.engine.model.trainable_parameters()` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `drift_sq = mx.array(0.0)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `matched = 0` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `for key, before_value in initial_trainable.items():` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `after_value = current_trainable.get(key)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `if after_value is None:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `continue` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `diff = after_value - before_value` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `drift_sq = drift_sq + mx.sum(` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `diff.astype(mx.float32) * diff.astype(mx.float32)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `matched += 1` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `if matched <= 0:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `raise RuntimeError("Phase 3 could not match post-update LoRA trainables")` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `mx.eval(drift_sq)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `real_delta_l2 = float(mx.sqrt(drift_sq).item())` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `if real_delta_l2 <= 1e-12:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `raise RuntimeError("Phase 3 completed but measured zero real LoRA parameter delta")` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `blank-line formatting change within this audited hunk` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `if learn_consolidated_ids:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `try:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `self.engine.kg.mark_consolidated(learn_consolidated_ids)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `except Exception:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `pass` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `if rsi_consolidated_ids:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `try:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `self.engine.kg.mark_rsi_self_memories_consolidated(` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `rsi_consolidated_ids` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `).items()` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `}` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `self.engine.moe_manager.adapters_buffer_b = current` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `self.engine.moe_manager.swap_buffers_atomic()` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `_assert_same_model(self, model_identity, "Phase 3 buffer swap")` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `except Exception:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `pass` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `if learn_consolidated_ids:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `persisted = _save_rsi_adapter(self)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `print(` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `f"[✓] Phase 3 trained {updated} Learn/RSI memories in-place "` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `f"(||ΔW||₂={real_delta_l2:.8f}; raw-gradient fallback updates: "` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `f"{fallback_updates}; persisted={persisted}).",` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `flush=True,` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `return {` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `"updated": updated > 0,` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `"memories": updated,` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `"fallback_updates": fallback_updates,` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `"persisted": persisted,` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `"real_trainable_delta_l2": real_delta_l2,` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `}` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `finally:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `self.engine.kg.mark_consolidated(learn_consolidated_ids)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `initial_trainable.clear()` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `if rsi_consolidated_ids:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `if not was_training:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `try:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `self.engine.model.eval()` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `except Exception:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `pass` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `gc.collect(2)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `self.engine.kg.mark_rsi_self_memories_consolidated(rsi_consolidated_ids)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **ADD** `mx.clear_cache()` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `persisted = _save_rsi_adapter(self)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `print(` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `f"[✓] Phase 3 trained {updated} Learn/RSI memories in-place "` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `f"(raw-gradient fallback updates: {fallback_updates}; persisted={persisted}).",` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `flush=True,` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `return {` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `"updated": updated > 0,` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `"memories": updated,` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `"fallback_updates": fallback_updates,` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `"persisted": persisted,` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `}` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.
- **REMOVE** `blank-line formatting change within this audited hunk` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -720,111 +1157,236 @@ def _run_phase3_consolidation(self) -> Dict[str, Any]:`.

### `@@ -1022,8 +1584,15 @@ def missed_only_evaluate_all(self, splits, cache, phase, start, total):`

- **REMOVE** `if not MLX_AVAILABLE or self.engine.model is None or self.engine.tokenizer is None:` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -1022,8 +1584,15 @@ def missed_only_evaluate_all(self, splits, cache, phase, start, total):`.
- **REMOVE** `raise RuntimeError("Real 27B MLX model is not loaded; benchmark will not start offline")` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -1022,8 +1584,15 @@ def missed_only_evaluate_all(self, splits, cache, phase, start, total):`.
- **ADD** `app_backend = str(getattr(self.engine, "backend_key", "") or "").strip().lower()` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -1022,8 +1584,15 @@ def missed_only_evaluate_all(self, splits, cache, phase, start, total):`.
- **ADD** `if (` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -1022,8 +1584,15 @@ def missed_only_evaluate_all(self, splits, cache, phase, start, total):`.
- **ADD** `(not MLX_AVAILABLE and not app_backend)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -1022,8 +1584,15 @@ def missed_only_evaluate_all(self, splits, cache, phase, start, total):`.
- **ADD** `or self.engine.model is None` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -1022,8 +1584,15 @@ def missed_only_evaluate_all(self, splits, cache, phase, start, total):`.
- **ADD** `or self.engine.tokenizer is None` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -1022,8 +1584,15 @@ def missed_only_evaluate_all(self, splits, cache, phase, start, total):`.
- **ADD** `):` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -1022,8 +1584,15 @@ def missed_only_evaluate_all(self, splits, cache, phase, start, total):`.
- **ADD** `raise RuntimeError(` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -1022,8 +1584,15 @@ def missed_only_evaluate_all(self, splits, cache, phase, start, total):`.
- **ADD** `"A real supported text model/backend must be loaded; benchmark will not start offline"` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -1022,8 +1584,15 @@ def missed_only_evaluate_all(self, splits, cache, phase, start, total):`.
- **ADD** `)` — answer-blind RSI feedback plus bounded completion-only Phase 3, cumulative telemetry, EWC/OGP and real drift proof; exact location: `@@ -1022,8 +1584,15 @@ def missed_only_evaluate_all(self, splits, cache, phase, start, total):`.

## `eval/rsi_generation_memory_hardening.py` — modified, +16/-2

**Shared rationale:** import-safe non-MLX startup and synchronized pressure cleanup with full-precision KV.


### `@@ -30,7 +30,14 @@`

- **REMOVE** `from mlx_lm.models.cache import make_prompt_cache` — import-safe non-MLX startup and synchronized pressure cleanup with full-precision KV; exact location: `@@ -30,7 +30,14 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — import-safe non-MLX startup and synchronized pressure cleanup with full-precision KV; exact location: `@@ -30,7 +30,14 @@`.
- **ADD** `try:` — import-safe non-MLX startup and synchronized pressure cleanup with full-precision KV; exact location: `@@ -30,7 +30,14 @@`.
- **ADD** `from mlx_lm.models.cache import make_prompt_cache` — import-safe non-MLX startup and synchronized pressure cleanup with full-precision KV; exact location: `@@ -30,7 +30,14 @@`.
- **ADD** `except Exception:` — import-safe non-MLX startup and synchronized pressure cleanup with full-precision KV; exact location: `@@ -30,7 +30,14 @@`.
- **ADD** `# Canonical suite imports this module on every platform. The app bridge` — import-safe non-MLX startup and synchronized pressure cleanup with full-precision KV; exact location: `@@ -30,7 +30,14 @@`.
- **ADD** `# replaces this MLX-only branch path for non-MLX backends after import.` — import-safe non-MLX startup and synchronized pressure cleanup with full-precision KV; exact location: `@@ -30,7 +30,14 @@`.
- **ADD** `make_prompt_cache = None` — import-safe non-MLX startup and synchronized pressure cleanup with full-precision KV; exact location: `@@ -30,7 +30,14 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — import-safe non-MLX startup and synchronized pressure cleanup with full-precision KV; exact location: `@@ -30,7 +30,14 @@`.

### `@@ -61,6 +68,12 @@ def _clear_runtime_memory(mx: Any, *, force: bool = False) -> None:`

- **ADD** `try:` — import-safe non-MLX startup and synchronized pressure cleanup with full-precision KV; exact location: `@@ -61,6 +68,12 @@ def _clear_runtime_memory(mx: Any, *, force: bool = False) -> None:`.
- **ADD** `sync = getattr(mx, "synchronize", None)` — import-safe non-MLX startup and synchronized pressure cleanup with full-precision KV; exact location: `@@ -61,6 +68,12 @@ def _clear_runtime_memory(mx: Any, *, force: bool = False) -> None:`.
- **ADD** `if callable(sync):` — import-safe non-MLX startup and synchronized pressure cleanup with full-precision KV; exact location: `@@ -61,6 +68,12 @@ def _clear_runtime_memory(mx: Any, *, force: bool = False) -> None:`.
- **ADD** `sync()` — import-safe non-MLX startup and synchronized pressure cleanup with full-precision KV; exact location: `@@ -61,6 +68,12 @@ def _clear_runtime_memory(mx: Any, *, force: bool = False) -> None:`.
- **ADD** `except Exception:` — import-safe non-MLX startup and synchronized pressure cleanup with full-precision KV; exact location: `@@ -61,6 +68,12 @@ def _clear_runtime_memory(mx: Any, *, force: bool = False) -> None:`.
- **ADD** `pass` — import-safe non-MLX startup and synchronized pressure cleanup with full-precision KV; exact location: `@@ -61,6 +68,12 @@ def _clear_runtime_memory(mx: Any, *, force: bool = False) -> None:`.

### `@@ -146,10 +159,11 @@ def safe_generate_branches_same_model(`

- **ADD** `or make_prompt_cache is None` — import-safe non-MLX startup and synchronized pressure cleanup with full-precision KV; exact location: `@@ -146,10 +159,11 @@ def safe_generate_branches_same_model(`.
- **REMOVE** `raise RuntimeError("MLX model/tokenizer unavailable for RSI/Pro branching")` — import-safe non-MLX startup and synchronized pressure cleanup with full-precision KV; exact location: `@@ -146,10 +159,11 @@ def safe_generate_branches_same_model(`.
- **ADD** `raise RuntimeError("MLX model/tokenizer/cache unavailable for RSI/Pro branching")` — import-safe non-MLX startup and synchronized pressure cleanup with full-precision KV; exact location: `@@ -146,10 +159,11 @@ def safe_generate_branches_same_model(`.

## `eval/rsi_legacy_training_hardening.py` — modified, +71/-48

**Shared rationale:** keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval.


### `@@ -203,7 +203,13 @@ def install(p4: Any) -> None:`

- **REMOVE** `if not getattr(p4, "MLX_AVAILABLE", False) or self.engine.model is None:` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -203,7 +203,13 @@ def install(p4: Any) -> None:`.
- **ADD** `# Non-MLX app evals use the production backend's own transactional trainer.` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -203,7 +203,13 @@ def install(p4: Any) -> None:`.
- **ADD** `# Never run MLX tensor snapshot/EWC monkeypatches against GGUF/BitNet/Torch.` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -203,7 +203,13 @@ def install(p4: Any) -> None:`.
- **ADD** `if (` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -203,7 +203,13 @@ def install(p4: Any) -> None:`.
- **ADD** `str(getattr(self.engine, "backend_key", "mlx") or "mlx").lower() != "mlx"` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -203,7 +203,13 @@ def install(p4: Any) -> None:`.
- **ADD** `or not getattr(p4, "MLX_AVAILABLE", False)` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -203,7 +203,13 @@ def install(p4: Any) -> None:`.
- **ADD** `or self.engine.model is None` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -203,7 +203,13 @@ def install(p4: Any) -> None:`.
- **ADD** `):` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -203,7 +203,13 @@ def install(p4: Any) -> None:`.

### `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`

- **ADD** `# New bounded Phase-3 code handles completion-only target selection itself` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **ADD** `# and consumes this EWC context directly. Older Phase-3 implementations keep` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **ADD** `# the historical tokenizer/loss monkeypatch path below.` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **ADD** `bounded = callable(getattr(p4, "_phase3_bounded_gradients", None))` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **REMOVE** `def completion_only_cross_entropy(logits, targets, *args, **kwargs):` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **REMOVE** `losses = original_ce(logits, targets, *args, **kwargs)` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **REMOVE** `if not mask_state.get("completion_mask_active"):` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **REMOVE** `return losses` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **REMOVE** `start = max(0, int(mask_state.get("loss_start", 0) or 0))` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **REMOVE** `shape = tuple(getattr(losses, "shape", ()) or ())` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **REMOVE** `if not shape:` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **REMOVE** `return losses` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **REMOVE** `seq = int(shape[-1])` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **REMOVE** `if seq <= 1 or start <= 0:` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **REMOVE** `return losses` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **REMOVE** `start = min(start, seq - 1)` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **REMOVE** `return losses[..., start:]` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **REMOVE** `blank-line formatting change within this audited hunk` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **REMOVE** `def ewc_value_and_grad(model, lossfn):` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **REMOVE** `if not fisher or not snapshot or ewc_lambda <= 0.0:` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **REMOVE** `return original_value_and_grad(model, lossfn)` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **REMOVE** `blank-line formatting change within this audited hunk` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **REMOVE** `def protected_loss(current_model):` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **REMOVE** `base_loss = lossfn(current_model)` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **REMOVE** `current = dict(` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **REMOVE** `p4.mlx.utils.tree_flatten(current_model.trainable_parameters())` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **REMOVE** `)` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **REMOVE** `penalty = p4.mx.array(0.0)` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **REMOVE** `matched = 0` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **REMOVE** `for key, weight in current.items():` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **REMOVE** `f_k = fisher.get(key)` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **REMOVE** `ref = snapshot.get(key)` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **REMOVE** `if f_k is None or ref is None:` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **REMOVE** `continue` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **REMOVE** `diff = weight - ref` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **REMOVE** `penalty = penalty + p4.mx.sum(f_k * (diff ** 2))` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **REMOVE** `matched += 1` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **REMOVE** `if matched == 0:` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **REMOVE** `return base_loss` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **REMOVE** `return base_loss + (ewc_lambda / 2.0) * penalty` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **REMOVE** `blank-line formatting change within this audited hunk` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **REMOVE** `return original_value_and_grad(model, protected_loss)` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **REMOVE** `blank-line formatting change within this audited hunk` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **REMOVE** `self.engine.tokenizer = proxy` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **REMOVE** `p4.nn.losses.cross_entropy = completion_only_cross_entropy` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **REMOVE** `p4.nn.value_and_grad = ewc_value_and_grad` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **ADD** `if bounded:` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **ADD** `self._phase3_ewc_context = {` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **ADD** `"fisher": fisher,` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **ADD** `"reference": snapshot,` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **ADD** `"lambda": float(ewc_lambda),` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **ADD** `}` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **ADD** `else:` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **ADD** `def completion_only_cross_entropy(logits, targets, *args, **kwargs):` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **ADD** `losses = original_ce(logits, targets, *args, **kwargs)` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **ADD** `if not mask_state.get("completion_mask_active"):` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **ADD** `return losses` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **ADD** `start = max(0, int(mask_state.get("loss_start", 0) or 0))` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **ADD** `shape = tuple(getattr(losses, "shape", ()) or ())` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **ADD** `if not shape:` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **ADD** `return losses` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **ADD** `seq = int(shape[-1])` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **ADD** `if seq <= 1 or start <= 0:` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **ADD** `return losses` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **ADD** `start = min(start, seq - 1)` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **ADD** `return losses[..., start:]` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **ADD** `blank-line formatting change within this audited hunk` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **ADD** `def ewc_value_and_grad(model, lossfn):` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **ADD** `if not fisher or not snapshot or ewc_lambda <= 0.0:` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **ADD** `return original_value_and_grad(model, lossfn)` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **ADD** `blank-line formatting change within this audited hunk` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **ADD** `def protected_loss(current_model):` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **ADD** `base_loss = lossfn(current_model)` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **ADD** `current = dict(` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **ADD** `p4.mlx.utils.tree_flatten(current_model.trainable_parameters())` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **ADD** `)` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **ADD** `penalty = p4.mx.array(0.0)` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **ADD** `matched = 0` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **ADD** `for key, weight in current.items():` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **ADD** `f_k = fisher.get(key)` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **ADD** `ref = snapshot.get(key)` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **ADD** `if f_k is None or ref is None:` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **ADD** `continue` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **ADD** `diff = weight - ref` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **ADD** `penalty = penalty + p4.mx.sum(f_k * (diff ** 2))` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **ADD** `matched += 1` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **ADD** `if matched == 0:` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **ADD** `return base_loss` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **ADD** `return base_loss + (ewc_lambda / 2.0) * penalty` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **ADD** `blank-line formatting change within this audited hunk` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **ADD** `return original_value_and_grad(model, protected_loss)` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **ADD** `blank-line formatting change within this audited hunk` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **ADD** `self.engine.tokenizer = proxy` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **ADD** `p4.nn.losses.cross_entropy = completion_only_cross_entropy` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **ADD** `p4.nn.value_and_grad = ewc_value_and_grad` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.
- **ADD** `blank-line formatting change within this audited hunk` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -230,54 +236,66 @@ def transactional_completion_only_phase3(self):`.

### `@@ -289,10 +307,9 @@ def protected_loss(current_model):`

- **ADD** `result["bounded_phase3"] = bool(bounded)` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -289,10 +307,9 @@ def protected_loss(current_model):`.
- **REMOVE** `# Roll back model, dual-buffer, persisted adapter and DB state so retrying` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -289,10 +307,9 @@ def protected_loss(current_model):`.
- **REMOVE** `# Phase 3 begins from the exact pre-update state.` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -289,10 +307,9 @@ def protected_loss(current_model):`.

### `@@ -308,9 +325,15 @@ def protected_loss(current_model):`

- **REMOVE** `p4.nn.value_and_grad = original_value_and_grad` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -308,9 +325,15 @@ def protected_loss(current_model):`.
- **REMOVE** `p4.nn.losses.cross_entropy = original_ce` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -308,9 +325,15 @@ def protected_loss(current_model):`.
- **REMOVE** `self.engine.tokenizer = original_tokenizer` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -308,9 +325,15 @@ def protected_loss(current_model):`.
- **ADD** `if bounded:` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -308,9 +325,15 @@ def protected_loss(current_model):`.
- **ADD** `try:` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -308,9 +325,15 @@ def protected_loss(current_model):`.
- **ADD** `delattr(self, "_phase3_ewc_context")` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -308,9 +325,15 @@ def protected_loss(current_model):`.
- **ADD** `except Exception:` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -308,9 +325,15 @@ def protected_loss(current_model):`.
- **ADD** `pass` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -308,9 +325,15 @@ def protected_loss(current_model):`.
- **ADD** `else:` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -308,9 +325,15 @@ def protected_loss(current_model):`.
- **ADD** `p4.nn.value_and_grad = original_value_and_grad` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -308,9 +325,15 @@ def protected_loss(current_model):`.
- **ADD** `p4.nn.losses.cross_entropy = original_ce` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -308,9 +325,15 @@ def protected_loss(current_model):`.
- **ADD** `self.engine.tokenizer = original_tokenizer` — keep transactional/EWC legacy protection compatible with bounded Phase 3 and non-MLX Eval; exact location: `@@ -308,9 +325,15 @@ def protected_loss(current_model):`.

## `eval/rsi_resume_hardening.py` — modified, +33/-34

**Shared rationale:** resume only question-free self traces, never legacy prompt/reward RSI rows.


### `@@ -73,27 +73,20 @@ def _db_path(self) -> str:`

- **REMOVE** `def _existing_rsi_rows(self, session_id: str) -> Dict[Tuple[str, str], Dict[str, Any]]:` — resume only question-free self traces, never legacy prompt/reward RSI rows; exact location: `@@ -73,27 +73,20 @@ def _db_path(self) -> str:`.
- **ADD** `def _existing_rsi_traces(self) -> set[str]:` — resume only question-free self traces, never legacy prompt/reward RSI rows; exact location: `@@ -73,27 +73,20 @@ def _db_path(self) -> str:`.
- **ADD** `"""Return only question-free/reward-free RSI self traces eligible for Phase 3."""` — resume only question-free self traces, never legacy prompt/reward RSI rows; exact location: `@@ -73,27 +73,20 @@ def _db_path(self) -> str:`.
- **REMOVE** `return {}` — resume only question-free self traces, never legacy prompt/reward RSI rows; exact location: `@@ -73,27 +73,20 @@ def _db_path(self) -> str:`.
- **ADD** `return set()` — resume only question-free self traces, never legacy prompt/reward RSI rows; exact location: `@@ -73,27 +73,20 @@ def _db_path(self) -> str:`.
- **REMOVE** `conn.row_factory = sqlite3.Row` — resume only question-free self traces, never legacy prompt/reward RSI rows; exact location: `@@ -73,27 +73,20 @@ def _db_path(self) -> str:`.
- **REMOVE** `"SELECT * FROM episodic_interactions WHERE session_id=? AND consolidated=0 ORDER BY id ASC",` — resume only question-free self traces, never legacy prompt/reward RSI rows; exact location: `@@ -73,27 +73,20 @@ def _db_path(self) -> str:`.
- **REMOVE** `(session_id,),` — resume only question-free self traces, never legacy prompt/reward RSI rows; exact location: `@@ -73,27 +73,20 @@ def _db_path(self) -> str:`.
- **ADD** `"SELECT trace FROM rsi_self_memories "` — resume only question-free self traces, never legacy prompt/reward RSI rows; exact location: `@@ -73,27 +73,20 @@ def _db_path(self) -> str:`.
- **ADD** `"WHERE consolidated=0 ORDER BY id ASC"` — resume only question-free self traces, never legacy prompt/reward RSI rows; exact location: `@@ -73,27 +73,20 @@ def _db_path(self) -> str:`.
- **REMOVE** `return {}` — resume only question-free self traces, never legacy prompt/reward RSI rows; exact location: `@@ -73,27 +73,20 @@ def _db_path(self) -> str:`.
- **REMOVE** `blank-line formatting change within this audited hunk` — resume only question-free self traces, never legacy prompt/reward RSI rows; exact location: `@@ -73,27 +73,20 @@ def _db_path(self) -> str:`.
- **REMOVE** `found: Dict[Tuple[str, str], Dict[str, Any]] = {}` — resume only question-free self traces, never legacy prompt/reward RSI rows; exact location: `@@ -73,27 +73,20 @@ def _db_path(self) -> str:`.
- **REMOVE** `for row in rows:` — resume only question-free self traces, never legacy prompt/reward RSI rows; exact location: `@@ -73,27 +73,20 @@ def _db_path(self) -> str:`.
- **REMOVE** `record = dict(row)` — resume only question-free self traces, never legacy prompt/reward RSI rows; exact location: `@@ -73,27 +73,20 @@ def _db_path(self) -> str:`.
- **REMOVE** `domain = str(record.get("domain", ""))` — resume only question-free self traces, never legacy prompt/reward RSI rows; exact location: `@@ -73,27 +73,20 @@ def _db_path(self) -> str:`.
- **REMOVE** `if domain.startswith("RSI::"):` — resume only question-free self traces, never legacy prompt/reward RSI rows; exact location: `@@ -73,27 +73,20 @@ def _db_path(self) -> str:`.
- **REMOVE** `found[(domain[len("RSI::"):], str(record.get("prompt", "")))] = record` — resume only question-free self traces, never legacy prompt/reward RSI rows; exact location: `@@ -73,27 +73,20 @@ def _db_path(self) -> str:`.
- **REMOVE** `return found` — resume only question-free self traces, never legacy prompt/reward RSI rows; exact location: `@@ -73,27 +73,20 @@ def _db_path(self) -> str:`.
- **ADD** `return set()` — resume only question-free self traces, never legacy prompt/reward RSI rows; exact location: `@@ -73,27 +73,20 @@ def _db_path(self) -> str:`.
- **ADD** `return {str(row[0]) for row in rows if str(row[0] or "").strip()}` — resume only question-free self traces, never legacy prompt/reward RSI rows; exact location: `@@ -73,27 +73,20 @@ def _db_path(self) -> str:`.

### `@@ -201,7 +194,7 @@ def resumable_rsi(self, splits, cache) -> int:`

- **REMOVE** `existing_rows = _existing_rsi_rows(self, p4.RSI_SESSION_ID)` — resume only question-free self traces, never legacy prompt/reward RSI rows; exact location: `@@ -201,7 +194,7 @@ def resumable_rsi(self, splits, cache) -> int:`.
- **ADD** `existing_traces = _existing_rsi_traces(self)` — resume only question-free self traces, never legacy prompt/reward RSI rows; exact location: `@@ -201,7 +194,7 @@ def resumable_rsi(self, splits, cache) -> int:`.

### `@@ -214,23 +207,18 @@ def resumable_rsi(self, splits, cache) -> int:`

- **REMOVE** `prompt = str(item.get("prompt", ""))` — resume only question-free self traces, never legacy prompt/reward RSI rows; exact location: `@@ -214,23 +207,18 @@ def resumable_rsi(self, splits, cache) -> int:`.
- **REMOVE** `if (split, prompt) in existing_rows:` — resume only question-free self traces, never legacy prompt/reward RSI rows; exact location: `@@ -214,23 +207,18 @@ def resumable_rsi(self, splits, cache) -> int:`.
- **REMOVE** `continue` — resume only question-free self traces, never legacy prompt/reward RSI rows; exact location: `@@ -214,23 +207,18 @@ def resumable_rsi(self, splits, cache) -> int:`.
- **ADD** `if candidate in existing_traces:` — resume only question-free self traces, never legacy prompt/reward RSI rows; exact location: `@@ -214,23 +207,18 @@ def resumable_rsi(self, splits, cache) -> int:`.
- **ADD** `continue` — resume only question-free self traces, never legacy prompt/reward RSI rows; exact location: `@@ -214,23 +207,18 @@ def resumable_rsi(self, splits, cache) -> int:`.
- **REMOVE** `self.engine.kg.log_interaction(` — resume only question-free self traces, never legacy prompt/reward RSI rows; exact location: `@@ -214,23 +207,18 @@ def resumable_rsi(self, splits, cache) -> int:`.
- **REMOVE** `p4.RSI_SESSION_ID,` — resume only question-free self traces, never legacy prompt/reward RSI rows; exact location: `@@ -214,23 +207,18 @@ def resumable_rsi(self, splits, cache) -> int:`.
- **REMOVE** `prompt,` — resume only question-free self traces, never legacy prompt/reward RSI rows; exact location: `@@ -214,23 +207,18 @@ def resumable_rsi(self, splits, cache) -> int:`.
- **REMOVE** `candidate,` — resume only question-free self traces, never legacy prompt/reward RSI rows; exact location: `@@ -214,23 +207,18 @@ def resumable_rsi(self, splits, cache) -> int:`.
- **REMOVE** `1.0,` — resume only question-free self traces, never legacy prompt/reward RSI rows; exact location: `@@ -214,23 +207,18 @@ def resumable_rsi(self, splits, cache) -> int:`.
- **REMOVE** `1.0,` — resume only question-free self traces, never legacy prompt/reward RSI rows; exact location: `@@ -214,23 +207,18 @@ def resumable_rsi(self, splits, cache) -> int:`.
- **REMOVE** `domain=f"RSI::{split}",` — resume only question-free self traces, never legacy prompt/reward RSI rows; exact location: `@@ -214,23 +207,18 @@ def resumable_rsi(self, splits, cache) -> int:`.
- **REMOVE** `)` — resume only question-free self traces, never legacy prompt/reward RSI rows; exact location: `@@ -214,23 +207,18 @@ def resumable_rsi(self, splits, cache) -> int:`.
- **REMOVE** `existing_rows[(split, prompt)] = {"completion": candidate}` — resume only question-free self traces, never legacy prompt/reward RSI rows; exact location: `@@ -214,23 +207,18 @@ def resumable_rsi(self, splits, cache) -> int:`.
- **ADD** `# Resume restores only the self-generated candidate trace. Benchmark` — resume only question-free self traces, never legacy prompt/reward RSI rows; exact location: `@@ -214,23 +207,18 @@ def resumable_rsi(self, splits, cache) -> int:`.
- **ADD** `# question, split, reward, PASS/FAIL and verifier metadata remain` — resume only question-free self traces, never legacy prompt/reward RSI rows; exact location: `@@ -214,23 +207,18 @@ def resumable_rsi(self, splits, cache) -> int:`.
- **ADD** `# checkpoint/log metadata and never become the persistent training row.` — resume only question-free self traces, never legacy prompt/reward RSI rows; exact location: `@@ -214,23 +207,18 @@ def resumable_rsi(self, splits, cache) -> int:`.
- **ADD** `self.engine.kg.log_rsi_self_memory(candidate)` — resume only question-free self traces, never legacy prompt/reward RSI rows; exact location: `@@ -214,23 +207,18 @@ def resumable_rsi(self, splits, cache) -> int:`.
- **ADD** `existing_traces.add(candidate)` — resume only question-free self traces, never legacy prompt/reward RSI rows; exact location: `@@ -214,23 +207,18 @@ def resumable_rsi(self, splits, cache) -> int:`.

### `@@ -264,14 +252,23 @@ def resumable_rsi(self, splits, cache) -> int:`

- **REMOVE** `original_delete = p4._delete_unconsumed_session_rows` — resume only question-free self traces, never legacy prompt/reward RSI rows; exact location: `@@ -264,14 +252,23 @@ def resumable_rsi(self, splits, cache) -> int:`.
- **REMOVE** `blank-line formatting change within this audited hunk` — resume only question-free self traces, never legacy prompt/reward RSI rows; exact location: `@@ -264,14 +252,23 @@ def resumable_rsi(self, splits, cache) -> int:`.
- **REMOVE** `def guarded_delete(owner, session_id):` — resume only question-free self traces, never legacy prompt/reward RSI rows; exact location: `@@ -264,14 +252,23 @@ def resumable_rsi(self, splits, cache) -> int:`.
- **REMOVE** `if session_id == p4.RSI_SESSION_ID and completed:` — resume only question-free self traces, never legacy prompt/reward RSI rows; exact location: `@@ -264,14 +252,23 @@ def resumable_rsi(self, splits, cache) -> int:`.
- **ADD** `kg = getattr(self.engine, "kg", None)` — resume only question-free self traces, never legacy prompt/reward RSI rows; exact location: `@@ -264,14 +252,23 @@ def resumable_rsi(self, splits, cache) -> int:`.
- **ADD** `original_clear_self = getattr(kg, "clear_unconsolidated_rsi_self_memories", None)` — resume only question-free self traces, never legacy prompt/reward RSI rows; exact location: `@@ -264,14 +252,23 @@ def resumable_rsi(self, splits, cache) -> int:`.
- **ADD** `has_resumed_passes = any(` — resume only question-free self traces, never legacy prompt/reward RSI rows; exact location: `@@ -264,14 +252,23 @@ def resumable_rsi(self, splits, cache) -> int:`.
- **ADD** `bool(status.get("passed", False))` — resume only question-free self traces, never legacy prompt/reward RSI rows; exact location: `@@ -264,14 +252,23 @@ def resumable_rsi(self, splits, cache) -> int:`.
- **ADD** `for status in completed.values()` — resume only question-free self traces, never legacy prompt/reward RSI rows; exact location: `@@ -264,14 +252,23 @@ def resumable_rsi(self, splits, cache) -> int:`.
- **ADD** `)` — resume only question-free self traces, never legacy prompt/reward RSI rows; exact location: `@@ -264,14 +252,23 @@ def resumable_rsi(self, splits, cache) -> int:`.
- **ADD** `blank-line formatting change within this audited hunk` — resume only question-free self traces, never legacy prompt/reward RSI rows; exact location: `@@ -264,14 +252,23 @@ def resumable_rsi(self, splits, cache) -> int:`.
- **ADD** `def guarded_clear_self():` — resume only question-free self traces, never legacy prompt/reward RSI rows; exact location: `@@ -264,14 +252,23 @@ def resumable_rsi(self, splits, cache) -> int:`.
- **ADD** `# Base RSI normally starts from a clean inbox. During resume, preserve` — resume only question-free self traces, never legacy prompt/reward RSI rows; exact location: `@@ -264,14 +252,23 @@ def resumable_rsi(self, splits, cache) -> int:`.
- **ADD** `# exact verified traces reconstructed above so Phase 3 can consume them.` — resume only question-free self traces, never legacy prompt/reward RSI rows; exact location: `@@ -264,14 +252,23 @@ def resumable_rsi(self, splits, cache) -> int:`.
- **ADD** `if has_resumed_passes:` — resume only question-free self traces, never legacy prompt/reward RSI rows; exact location: `@@ -264,14 +252,23 @@ def resumable_rsi(self, splits, cache) -> int:`.
- **REMOVE** `return original_delete(owner, session_id)` — resume only question-free self traces, never legacy prompt/reward RSI rows; exact location: `@@ -264,14 +252,23 @@ def resumable_rsi(self, splits, cache) -> int:`.
- **ADD** `if callable(original_clear_self):` — resume only question-free self traces, never legacy prompt/reward RSI rows; exact location: `@@ -264,14 +252,23 @@ def resumable_rsi(self, splits, cache) -> int:`.
- **ADD** `return original_clear_self()` — resume only question-free self traces, never legacy prompt/reward RSI rows; exact location: `@@ -264,14 +252,23 @@ def resumable_rsi(self, splits, cache) -> int:`.
- **ADD** `return None` — resume only question-free self traces, never legacy prompt/reward RSI rows; exact location: `@@ -264,14 +252,23 @@ def resumable_rsi(self, splits, cache) -> int:`.

### `@@ -307,15 +304,17 @@ def checkpointing_log(owner, split, item_id, round_idx, candidate, passed, selec`

- **REMOVE** `p4._delete_unconsumed_session_rows = guarded_delete` — resume only question-free self traces, never legacy prompt/reward RSI rows; exact location: `@@ -307,15 +304,17 @@ def checkpointing_log(owner, split, item_id, round_idx, candidate, passed, selec`.
- **ADD** `if kg is not None and callable(original_clear_self):` — resume only question-free self traces, never legacy prompt/reward RSI rows; exact location: `@@ -307,15 +304,17 @@ def checkpointing_log(owner, split, item_id, round_idx, candidate, passed, selec`.
- **ADD** `kg.clear_unconsolidated_rsi_self_memories = guarded_clear_self` — resume only question-free self traces, never legacy prompt/reward RSI rows; exact location: `@@ -307,15 +304,17 @@ def checkpointing_log(owner, split, item_id, round_idx, candidate, passed, selec`.
- **REMOVE** `p4._delete_unconsumed_session_rows = original_delete` — resume only question-free self traces, never legacy prompt/reward RSI rows; exact location: `@@ -307,15 +304,17 @@ def checkpointing_log(owner, split, item_id, round_idx, candidate, passed, selec`.
- **ADD** `if kg is not None and callable(original_clear_self):` — resume only question-free self traces, never legacy prompt/reward RSI rows; exact location: `@@ -307,15 +304,17 @@ def checkpointing_log(owner, split, item_id, round_idx, candidate, passed, selec`.
- **ADD** `kg.clear_unconsolidated_rsi_self_memories = original_clear_self` — resume only question-free self traces, never legacy prompt/reward RSI rows; exact location: `@@ -307,15 +304,17 @@ def checkpointing_log(owner, split, item_id, round_idx, candidate, passed, selec`.

## `eval/stage_integrity_telemetry.py` — modified, +46/-10

**Shared rationale:** validate separate Learn/RSI tables and post-training consolidation state.


### `@@ -308,15 +308,28 @@ def heartbeat():`

- **REMOVE** `learn_rows = [r for r in queued if str(r.get("session_id", "")) == str(p4.LEARN_SESSION_ID)]` — validate separate Learn/RSI tables and post-training consolidation state; exact location: `@@ -308,15 +308,28 @@ def heartbeat():`.
- **REMOVE** `rsi_rows = [r for r in queued if str(r.get("session_id", "")) == str(p4.RSI_SESSION_ID)]` — validate separate Learn/RSI tables and post-training consolidation state; exact location: `@@ -308,15 +308,28 @@ def heartbeat():`.
- **ADD** `learn_rows = [` — validate separate Learn/RSI tables and post-training consolidation state; exact location: `@@ -308,15 +308,28 @@ def heartbeat():`.
- **ADD** `r for r in queued` — validate separate Learn/RSI tables and post-training consolidation state; exact location: `@@ -308,15 +308,28 @@ def heartbeat():`.
- **ADD** `if str(r.get("memory_kind") or "") == "learn"` — validate separate Learn/RSI tables and post-training consolidation state; exact location: `@@ -308,15 +308,28 @@ def heartbeat():`.
- **ADD** `or str(r.get("session_id", "")) == str(p4.LEARN_SESSION_ID)` — validate separate Learn/RSI tables and post-training consolidation state; exact location: `@@ -308,15 +308,28 @@ def heartbeat():`.
- **ADD** `]` — validate separate Learn/RSI tables and post-training consolidation state; exact location: `@@ -308,15 +308,28 @@ def heartbeat():`.
- **ADD** `rsi_rows = [` — validate separate Learn/RSI tables and post-training consolidation state; exact location: `@@ -308,15 +308,28 @@ def heartbeat():`.
- **ADD** `r for r in queued` — validate separate Learn/RSI tables and post-training consolidation state; exact location: `@@ -308,15 +308,28 @@ def heartbeat():`.
- **ADD** `if str(r.get("memory_kind") or "") == "rsi_self"` — validate separate Learn/RSI tables and post-training consolidation state; exact location: `@@ -308,15 +308,28 @@ def heartbeat():`.
- **ADD** `or str(r.get("session_id", "")) == str(p4.RSI_SESSION_ID)` — validate separate Learn/RSI tables and post-training consolidation state; exact location: `@@ -308,15 +308,28 @@ def heartbeat():`.
- **ADD** `]` — validate separate Learn/RSI tables and post-training consolidation state; exact location: `@@ -308,15 +308,28 @@ def heartbeat():`.
- **REMOVE** `queued_ids = [int(r["id"]) for r in queued if r.get("id") is not None]` — validate separate Learn/RSI tables and post-training consolidation state; exact location: `@@ -308,15 +308,28 @@ def heartbeat():`.
- **ADD** `learn_ids = [` — validate separate Learn/RSI tables and post-training consolidation state; exact location: `@@ -308,15 +308,28 @@ def heartbeat():`.
- **ADD** `int(r["id"]) for r in learn_rows if r.get("id") is not None` — validate separate Learn/RSI tables and post-training consolidation state; exact location: `@@ -308,15 +308,28 @@ def heartbeat():`.
- **ADD** `]` — validate separate Learn/RSI tables and post-training consolidation state; exact location: `@@ -308,15 +308,28 @@ def heartbeat():`.
- **ADD** `rsi_ids = [` — validate separate Learn/RSI tables and post-training consolidation state; exact location: `@@ -308,15 +308,28 @@ def heartbeat():`.
- **ADD** `int(r["id"]) for r in rsi_rows if r.get("id") is not None` — validate separate Learn/RSI tables and post-training consolidation state; exact location: `@@ -308,15 +308,28 @@ def heartbeat():`.
- **ADD** `]` — validate separate Learn/RSI tables and post-training consolidation state; exact location: `@@ -308,15 +308,28 @@ def heartbeat():`.

### `@@ -342,17 +355,40 @@ def phase3_with_integrity(self) -> Dict[str, Any]:`

- **REMOVE** `if queued_ids:` — validate separate Learn/RSI tables and post-training consolidation state; exact location: `@@ -342,17 +355,40 @@ def phase3_with_integrity(self) -> Dict[str, Any]:`.
- **ADD** `if learn_ids or rsi_ids:` — validate separate Learn/RSI tables and post-training consolidation state; exact location: `@@ -342,17 +355,40 @@ def phase3_with_integrity(self) -> Dict[str, Any]:`.
- **ADD** `not_marked = []` — validate separate Learn/RSI tables and post-training consolidation state; exact location: `@@ -342,17 +355,40 @@ def phase3_with_integrity(self) -> Dict[str, Any]:`.
- **REMOVE** `marks = conn.execute(` — validate separate Learn/RSI tables and post-training consolidation state; exact location: `@@ -342,17 +355,40 @@ def phase3_with_integrity(self) -> Dict[str, Any]:`.
- **REMOVE** `f"SELECT id, consolidated FROM episodic_interactions WHERE id IN ({','.join('?' for _ in queued_ids)})",` — validate separate Learn/RSI tables and post-training consolidation state; exact location: `@@ -342,17 +355,40 @@ def phase3_with_integrity(self) -> Dict[str, Any]:`.
- **REMOVE** `queued_ids,` — validate separate Learn/RSI tables and post-training consolidation state; exact location: `@@ -342,17 +355,40 @@ def phase3_with_integrity(self) -> Dict[str, Any]:`.
- **REMOVE** `).fetchall()` — validate separate Learn/RSI tables and post-training consolidation state; exact location: `@@ -342,17 +355,40 @@ def phase3_with_integrity(self) -> Dict[str, Any]:`.
- **REMOVE** `not_marked = [row[0] for row in marks if int(row[1] or 0) != 1]` — validate separate Learn/RSI tables and post-training consolidation state; exact location: `@@ -342,17 +355,40 @@ def phase3_with_integrity(self) -> Dict[str, Any]:`.
- **ADD** `if learn_ids:` — validate separate Learn/RSI tables and post-training consolidation state; exact location: `@@ -342,17 +355,40 @@ def phase3_with_integrity(self) -> Dict[str, Any]:`.
- **ADD** `learn_marks = conn.execute(` — validate separate Learn/RSI tables and post-training consolidation state; exact location: `@@ -342,17 +355,40 @@ def phase3_with_integrity(self) -> Dict[str, Any]:`.
- **ADD** `f"SELECT id, consolidated FROM episodic_interactions "` — validate separate Learn/RSI tables and post-training consolidation state; exact location: `@@ -342,17 +355,40 @@ def phase3_with_integrity(self) -> Dict[str, Any]:`.
- **ADD** `f"WHERE id IN ({','.join('?' for _ in learn_ids)})",` — validate separate Learn/RSI tables and post-training consolidation state; exact location: `@@ -342,17 +355,40 @@ def phase3_with_integrity(self) -> Dict[str, Any]:`.
- **ADD** `learn_ids,` — validate separate Learn/RSI tables and post-training consolidation state; exact location: `@@ -342,17 +355,40 @@ def phase3_with_integrity(self) -> Dict[str, Any]:`.
- **ADD** `).fetchall()` — validate separate Learn/RSI tables and post-training consolidation state; exact location: `@@ -342,17 +355,40 @@ def phase3_with_integrity(self) -> Dict[str, Any]:`.
- **ADD** `learn_state = {int(row[0]): int(row[1] or 0) for row in learn_marks}` — validate separate Learn/RSI tables and post-training consolidation state; exact location: `@@ -342,17 +355,40 @@ def phase3_with_integrity(self) -> Dict[str, Any]:`.
- **ADD** `not_marked.extend(` — validate separate Learn/RSI tables and post-training consolidation state; exact location: `@@ -342,17 +355,40 @@ def phase3_with_integrity(self) -> Dict[str, Any]:`.
- **ADD** `f"learn:{mid}"` — validate separate Learn/RSI tables and post-training consolidation state; exact location: `@@ -342,17 +355,40 @@ def phase3_with_integrity(self) -> Dict[str, Any]:`.
- **ADD** `for mid in learn_ids` — validate separate Learn/RSI tables and post-training consolidation state; exact location: `@@ -342,17 +355,40 @@ def phase3_with_integrity(self) -> Dict[str, Any]:`.
- **ADD** `if learn_state.get(mid) != 1` — validate separate Learn/RSI tables and post-training consolidation state; exact location: `@@ -342,17 +355,40 @@ def phase3_with_integrity(self) -> Dict[str, Any]:`.
- **ADD** `)` — validate separate Learn/RSI tables and post-training consolidation state; exact location: `@@ -342,17 +355,40 @@ def phase3_with_integrity(self) -> Dict[str, Any]:`.
- **ADD** `if rsi_ids:` — validate separate Learn/RSI tables and post-training consolidation state; exact location: `@@ -342,17 +355,40 @@ def phase3_with_integrity(self) -> Dict[str, Any]:`.
- **ADD** `rsi_marks = conn.execute(` — validate separate Learn/RSI tables and post-training consolidation state; exact location: `@@ -342,17 +355,40 @@ def phase3_with_integrity(self) -> Dict[str, Any]:`.
- **ADD** `f"SELECT id, consolidated FROM rsi_self_memories "` — validate separate Learn/RSI tables and post-training consolidation state; exact location: `@@ -342,17 +355,40 @@ def phase3_with_integrity(self) -> Dict[str, Any]:`.
- **ADD** `f"WHERE id IN ({','.join('?' for _ in rsi_ids)})",` — validate separate Learn/RSI tables and post-training consolidation state; exact location: `@@ -342,17 +355,40 @@ def phase3_with_integrity(self) -> Dict[str, Any]:`.
- **ADD** `rsi_ids,` — validate separate Learn/RSI tables and post-training consolidation state; exact location: `@@ -342,17 +355,40 @@ def phase3_with_integrity(self) -> Dict[str, Any]:`.
- **ADD** `).fetchall()` — validate separate Learn/RSI tables and post-training consolidation state; exact location: `@@ -342,17 +355,40 @@ def phase3_with_integrity(self) -> Dict[str, Any]:`.
- **ADD** `rsi_state = {int(row[0]): int(row[1] or 0) for row in rsi_marks}` — validate separate Learn/RSI tables and post-training consolidation state; exact location: `@@ -342,17 +355,40 @@ def phase3_with_integrity(self) -> Dict[str, Any]:`.
- **ADD** `not_marked.extend(` — validate separate Learn/RSI tables and post-training consolidation state; exact location: `@@ -342,17 +355,40 @@ def phase3_with_integrity(self) -> Dict[str, Any]:`.
- **ADD** `f"rsi:{mid}"` — validate separate Learn/RSI tables and post-training consolidation state; exact location: `@@ -342,17 +355,40 @@ def phase3_with_integrity(self) -> Dict[str, Any]:`.
- **ADD** `for mid in rsi_ids` — validate separate Learn/RSI tables and post-training consolidation state; exact location: `@@ -342,17 +355,40 @@ def phase3_with_integrity(self) -> Dict[str, Any]:`.
- **ADD** `if rsi_state.get(mid) != 1` — validate separate Learn/RSI tables and post-training consolidation state; exact location: `@@ -342,17 +355,40 @@ def phase3_with_integrity(self) -> Dict[str, Any]:`.
- **ADD** `)` — validate separate Learn/RSI tables and post-training consolidation state; exact location: `@@ -342,17 +355,40 @@ def phase3_with_integrity(self) -> Dict[str, Any]:`.
- **REMOVE** `raise RuntimeError(f"Phase 3 left {len(not_marked)} trained memories unconsolidated")` — validate separate Learn/RSI tables and post-training consolidation state; exact location: `@@ -342,17 +355,40 @@ def phase3_with_integrity(self) -> Dict[str, Any]:`.
- **ADD** `raise RuntimeError(` — validate separate Learn/RSI tables and post-training consolidation state; exact location: `@@ -342,17 +355,40 @@ def phase3_with_integrity(self) -> Dict[str, Any]:`.
- **ADD** `f"Phase 3 left {len(not_marked)} trained memories unconsolidated: "` — validate separate Learn/RSI tables and post-training consolidation state; exact location: `@@ -342,17 +355,40 @@ def phase3_with_integrity(self) -> Dict[str, Any]:`.
- **ADD** `+ ", ".join(not_marked[:8])` — validate separate Learn/RSI tables and post-training consolidation state; exact location: `@@ -342,17 +355,40 @@ def phase3_with_integrity(self) -> Dict[str, Any]:`.
- **ADD** `)` — validate separate Learn/RSI tables and post-training consolidation state; exact location: `@@ -342,17 +355,40 @@ def phase3_with_integrity(self) -> Dict[str, Any]:`.

## `eval/swe_verifier_hardening.py` — modified, +55/-15

**Shared rationale:** preserve verifier semantics while making patch apply portable.


### `@@ -68,38 +68,78 @@ def verify_git_diff_patch(self, repo_structure, patch_text, test_cmd):`

- **REMOVE** `if use_git_apply:` — preserve verifier semantics while making patch apply portable; exact location: `@@ -68,38 +68,78 @@ def verify_git_diff_patch(self, repo_structure, patch_text, test_cmd):`.
- **ADD** `# Windows does not normally ship the POSIX \`patch\` utility.` — preserve verifier semantics while making patch apply portable; exact location: `@@ -68,38 +68,78 @@ def verify_git_diff_patch(self, repo_structure, patch_text, test_cmd):`.
- **ADD** `# Keep the historical Unix path when present, otherwise use Git's` — preserve verifier semantics while making patch apply portable; exact location: `@@ -68,38 +68,78 @@ def verify_git_diff_patch(self, repo_structure, patch_text, test_cmd):`.
- **ADD** `# cross-platform unified-diff parser.` — preserve verifier semantics while making patch apply portable; exact location: `@@ -68,38 +68,78 @@ def verify_git_diff_patch(self, repo_structure, patch_text, test_cmd):`.
- **ADD** `git_bin = shutil.which("git")` — preserve verifier semantics while making patch apply portable; exact location: `@@ -68,38 +68,78 @@ def verify_git_diff_patch(self, repo_structure, patch_text, test_cmd):`.
- **ADD** `patch_bin = shutil.which("patch")` — preserve verifier semantics while making patch apply portable; exact location: `@@ -68,38 +68,78 @@ def verify_git_diff_patch(self, repo_structure, patch_text, test_cmd):`.
- **ADD** `use_git = bool(` — preserve verifier semantics while making patch apply portable; exact location: `@@ -68,38 +68,78 @@ def verify_git_diff_patch(self, repo_structure, patch_text, test_cmd):`.
- **ADD** `use_git_apply` — preserve verifier semantics while making patch apply portable; exact location: `@@ -68,38 +68,78 @@ def verify_git_diff_patch(self, repo_structure, patch_text, test_cmd):`.
- **ADD** `or platform.system() == "Windows"` — preserve verifier semantics while making patch apply portable; exact location: `@@ -68,38 +68,78 @@ def verify_git_diff_patch(self, repo_structure, patch_text, test_cmd):`.
- **ADD** `or not patch_bin` — preserve verifier semantics while making patch apply portable; exact location: `@@ -68,38 +68,78 @@ def verify_git_diff_patch(self, repo_structure, patch_text, test_cmd):`.
- **ADD** `)` — preserve verifier semantics while making patch apply portable; exact location: `@@ -68,38 +68,78 @@ def verify_git_diff_patch(self, repo_structure, patch_text, test_cmd):`.
- **ADD** `blank-line formatting change within this audited hunk` — preserve verifier semantics while making patch apply portable; exact location: `@@ -68,38 +68,78 @@ def verify_git_diff_patch(self, repo_structure, patch_text, test_cmd):`.
- **ADD** `if use_git:` — preserve verifier semantics while making patch apply portable; exact location: `@@ -68,38 +68,78 @@ def verify_git_diff_patch(self, repo_structure, patch_text, test_cmd):`.
- **ADD** `if not git_bin:` — preserve verifier semantics while making patch apply portable; exact location: `@@ -68,38 +68,78 @@ def verify_git_diff_patch(self, repo_structure, patch_text, test_cmd):`.
- **ADD** `return result_cls(` — preserve verifier semantics while making patch apply portable; exact location: `@@ -68,38 +68,78 @@ def verify_git_diff_patch(self, repo_structure, patch_text, test_cmd):`.
- **ADD** `False,` — preserve verifier semantics while making patch apply portable; exact location: `@@ -68,38 +68,78 @@ def verify_git_diff_patch(self, repo_structure, patch_text, test_cmd):`.
- **ADD** `(time.perf_counter() - started) * 1000.0,` — preserve verifier semantics while making patch apply portable; exact location: `@@ -68,38 +68,78 @@ def verify_git_diff_patch(self, repo_structure, patch_text, test_cmd):`.
- **ADD** `"",` — preserve verifier semantics while making patch apply portable; exact location: `@@ -68,38 +68,78 @@ def verify_git_diff_patch(self, repo_structure, patch_text, test_cmd):`.
- **ADD** `"SWE patch verification requires Git (or POSIX patch on Unix).",` — preserve verifier semantics while making patch apply portable; exact location: `@@ -68,38 +68,78 @@ def verify_git_diff_patch(self, repo_structure, patch_text, test_cmd):`.
- **ADD** `0.0,` — preserve verifier semantics while making patch apply portable; exact location: `@@ -68,38 +68,78 @@ def verify_git_diff_patch(self, repo_structure, patch_text, test_cmd):`.
- **ADD** `)` — preserve verifier semantics while making patch apply portable; exact location: `@@ -68,38 +68,78 @@ def verify_git_diff_patch(self, repo_structure, patch_text, test_cmd):`.
- **REMOVE** `["git", "init", "-q"], cwd=root, capture_output=True,` — preserve verifier semantics while making patch apply portable; exact location: `@@ -68,38 +68,78 @@ def verify_git_diff_patch(self, repo_structure, patch_text, test_cmd):`.
- **REMOVE** `text=True, timeout=patch_timeout,` — preserve verifier semantics while making patch apply portable; exact location: `@@ -68,38 +68,78 @@ def verify_git_diff_patch(self, repo_structure, patch_text, test_cmd):`.
- **ADD** `[git_bin, "init", "-q"],` — preserve verifier semantics while making patch apply portable; exact location: `@@ -68,38 +68,78 @@ def verify_git_diff_patch(self, repo_structure, patch_text, test_cmd):`.
- **ADD** `cwd=root,` — preserve verifier semantics while making patch apply portable; exact location: `@@ -68,38 +68,78 @@ def verify_git_diff_patch(self, repo_structure, patch_text, test_cmd):`.
- **ADD** `capture_output=True,` — preserve verifier semantics while making patch apply portable; exact location: `@@ -68,38 +68,78 @@ def verify_git_diff_patch(self, repo_structure, patch_text, test_cmd):`.
- **ADD** `text=True,` — preserve verifier semantics while making patch apply portable; exact location: `@@ -68,38 +68,78 @@ def verify_git_diff_patch(self, repo_structure, patch_text, test_cmd):`.
- **ADD** `timeout=patch_timeout,` — preserve verifier semantics while making patch apply portable; exact location: `@@ -68,38 +68,78 @@ def verify_git_diff_patch(self, repo_structure, patch_text, test_cmd):`.
- **REMOVE** `["git", "apply", "--check", "task.patch"], cwd=root,` — preserve verifier semantics while making patch apply portable; exact location: `@@ -68,38 +68,78 @@ def verify_git_diff_patch(self, repo_structure, patch_text, test_cmd):`.
- **REMOVE** `capture_output=True, text=True, timeout=patch_timeout,` — preserve verifier semantics while making patch apply portable; exact location: `@@ -68,38 +68,78 @@ def verify_git_diff_patch(self, repo_structure, patch_text, test_cmd):`.
- **ADD** `[git_bin, "apply", "--check", "task.patch"],` — preserve verifier semantics while making patch apply portable; exact location: `@@ -68,38 +68,78 @@ def verify_git_diff_patch(self, repo_structure, patch_text, test_cmd):`.
- **ADD** `cwd=root,` — preserve verifier semantics while making patch apply portable; exact location: `@@ -68,38 +68,78 @@ def verify_git_diff_patch(self, repo_structure, patch_text, test_cmd):`.
- **ADD** `capture_output=True,` — preserve verifier semantics while making patch apply portable; exact location: `@@ -68,38 +68,78 @@ def verify_git_diff_patch(self, repo_structure, patch_text, test_cmd):`.
- **ADD** `text=True,` — preserve verifier semantics while making patch apply portable; exact location: `@@ -68,38 +68,78 @@ def verify_git_diff_patch(self, repo_structure, patch_text, test_cmd):`.
- **ADD** `timeout=patch_timeout,` — preserve verifier semantics while making patch apply portable; exact location: `@@ -68,38 +68,78 @@ def verify_git_diff_patch(self, repo_structure, patch_text, test_cmd):`.
- **REMOVE** `False, (time.perf_counter() - started) * 1000.0,` — preserve verifier semantics while making patch apply portable; exact location: `@@ -68,38 +68,78 @@ def verify_git_diff_patch(self, repo_structure, patch_text, test_cmd):`.
- **REMOVE** `check.stdout, check.stderr[-2000:], 0.0,` — preserve verifier semantics while making patch apply portable; exact location: `@@ -68,38 +68,78 @@ def verify_git_diff_patch(self, repo_structure, patch_text, test_cmd):`.
- **ADD** `False,` — preserve verifier semantics while making patch apply portable; exact location: `@@ -68,38 +68,78 @@ def verify_git_diff_patch(self, repo_structure, patch_text, test_cmd):`.
- **ADD** `(time.perf_counter() - started) * 1000.0,` — preserve verifier semantics while making patch apply portable; exact location: `@@ -68,38 +68,78 @@ def verify_git_diff_patch(self, repo_structure, patch_text, test_cmd):`.
- **ADD** `check.stdout,` — preserve verifier semantics while making patch apply portable; exact location: `@@ -68,38 +68,78 @@ def verify_git_diff_patch(self, repo_structure, patch_text, test_cmd):`.
- **ADD** `check.stderr[-2000:],` — preserve verifier semantics while making patch apply portable; exact location: `@@ -68,38 +68,78 @@ def verify_git_diff_patch(self, repo_structure, patch_text, test_cmd):`.
- **ADD** `0.0,` — preserve verifier semantics while making patch apply portable; exact location: `@@ -68,38 +68,78 @@ def verify_git_diff_patch(self, repo_structure, patch_text, test_cmd):`.
- **REMOVE** `["git", "apply", "task.patch"], cwd=root,` — preserve verifier semantics while making patch apply portable; exact location: `@@ -68,38 +68,78 @@ def verify_git_diff_patch(self, repo_structure, patch_text, test_cmd):`.
- **REMOVE** `capture_output=True, text=True, timeout=patch_timeout,` — preserve verifier semantics while making patch apply portable; exact location: `@@ -68,38 +68,78 @@ def verify_git_diff_patch(self, repo_structure, patch_text, test_cmd):`.
- **ADD** `[git_bin, "apply", "task.patch"],` — preserve verifier semantics while making patch apply portable; exact location: `@@ -68,38 +68,78 @@ def verify_git_diff_patch(self, repo_structure, patch_text, test_cmd):`.
- **ADD** `cwd=root,` — preserve verifier semantics while making patch apply portable; exact location: `@@ -68,38 +68,78 @@ def verify_git_diff_patch(self, repo_structure, patch_text, test_cmd):`.
- **ADD** `capture_output=True,` — preserve verifier semantics while making patch apply portable; exact location: `@@ -68,38 +68,78 @@ def verify_git_diff_patch(self, repo_structure, patch_text, test_cmd):`.
- **ADD** `text=True,` — preserve verifier semantics while making patch apply portable; exact location: `@@ -68,38 +68,78 @@ def verify_git_diff_patch(self, repo_structure, patch_text, test_cmd):`.
- **ADD** `timeout=patch_timeout,` — preserve verifier semantics while making patch apply portable; exact location: `@@ -68,38 +68,78 @@ def verify_git_diff_patch(self, repo_structure, patch_text, test_cmd):`.
- **REMOVE** `["patch", "--dry-run", f"-p{strip}", "-i", "task.patch"],` — preserve verifier semantics while making patch apply portable; exact location: `@@ -68,38 +68,78 @@ def verify_git_diff_patch(self, repo_structure, patch_text, test_cmd):`.
- **REMOVE** `cwd=root, capture_output=True, text=True, timeout=patch_timeout,` — preserve verifier semantics while making patch apply portable; exact location: `@@ -68,38 +68,78 @@ def verify_git_diff_patch(self, repo_structure, patch_text, test_cmd):`.
- **ADD** `[patch_bin, "--dry-run", f"-p{strip}", "-i", "task.patch"],` — preserve verifier semantics while making patch apply portable; exact location: `@@ -68,38 +68,78 @@ def verify_git_diff_patch(self, repo_structure, patch_text, test_cmd):`.
- **ADD** `cwd=root,` — preserve verifier semantics while making patch apply portable; exact location: `@@ -68,38 +68,78 @@ def verify_git_diff_patch(self, repo_structure, patch_text, test_cmd):`.
- **ADD** `capture_output=True,` — preserve verifier semantics while making patch apply portable; exact location: `@@ -68,38 +68,78 @@ def verify_git_diff_patch(self, repo_structure, patch_text, test_cmd):`.
- **ADD** `text=True,` — preserve verifier semantics while making patch apply portable; exact location: `@@ -68,38 +68,78 @@ def verify_git_diff_patch(self, repo_structure, patch_text, test_cmd):`.
- **ADD** `timeout=patch_timeout,` — preserve verifier semantics while making patch apply portable; exact location: `@@ -68,38 +68,78 @@ def verify_git_diff_patch(self, repo_structure, patch_text, test_cmd):`.
- **REMOVE** `False, (time.perf_counter() - started) * 1000.0,` — preserve verifier semantics while making patch apply portable; exact location: `@@ -68,38 +68,78 @@ def verify_git_diff_patch(self, repo_structure, patch_text, test_cmd):`.
- **REMOVE** `check.stdout, check.stderr[-2000:], 0.0,` — preserve verifier semantics while making patch apply portable; exact location: `@@ -68,38 +68,78 @@ def verify_git_diff_patch(self, repo_structure, patch_text, test_cmd):`.
- **ADD** `False,` — preserve verifier semantics while making patch apply portable; exact location: `@@ -68,38 +68,78 @@ def verify_git_diff_patch(self, repo_structure, patch_text, test_cmd):`.
- **ADD** `(time.perf_counter() - started) * 1000.0,` — preserve verifier semantics while making patch apply portable; exact location: `@@ -68,38 +68,78 @@ def verify_git_diff_patch(self, repo_structure, patch_text, test_cmd):`.
- **ADD** `check.stdout,` — preserve verifier semantics while making patch apply portable; exact location: `@@ -68,38 +68,78 @@ def verify_git_diff_patch(self, repo_structure, patch_text, test_cmd):`.
- **ADD** `check.stderr[-2000:],` — preserve verifier semantics while making patch apply portable; exact location: `@@ -68,38 +68,78 @@ def verify_git_diff_patch(self, repo_structure, patch_text, test_cmd):`.
- **ADD** `0.0,` — preserve verifier semantics while making patch apply portable; exact location: `@@ -68,38 +68,78 @@ def verify_git_diff_patch(self, repo_structure, patch_text, test_cmd):`.
- **REMOVE** `["patch", f"-p{strip}", "-i", "task.patch"], cwd=root,` — preserve verifier semantics while making patch apply portable; exact location: `@@ -68,38 +68,78 @@ def verify_git_diff_patch(self, repo_structure, patch_text, test_cmd):`.
- **REMOVE** `capture_output=True, text=True, timeout=patch_timeout,` — preserve verifier semantics while making patch apply portable; exact location: `@@ -68,38 +68,78 @@ def verify_git_diff_patch(self, repo_structure, patch_text, test_cmd):`.
- **ADD** `[patch_bin, f"-p{strip}", "-i", "task.patch"],` — preserve verifier semantics while making patch apply portable; exact location: `@@ -68,38 +68,78 @@ def verify_git_diff_patch(self, repo_structure, patch_text, test_cmd):`.
- **ADD** `cwd=root,` — preserve verifier semantics while making patch apply portable; exact location: `@@ -68,38 +68,78 @@ def verify_git_diff_patch(self, repo_structure, patch_text, test_cmd):`.
- **ADD** `capture_output=True,` — preserve verifier semantics while making patch apply portable; exact location: `@@ -68,38 +68,78 @@ def verify_git_diff_patch(self, repo_structure, patch_text, test_cmd):`.
- **ADD** `text=True,` — preserve verifier semantics while making patch apply portable; exact location: `@@ -68,38 +68,78 @@ def verify_git_diff_patch(self, repo_structure, patch_text, test_cmd):`.
- **ADD** `timeout=patch_timeout,` — preserve verifier semantics while making patch apply portable; exact location: `@@ -68,38 +68,78 @@ def verify_git_diff_patch(self, repo_structure, patch_text, test_cmd):`.

## `master_4000_eval_suite.py` — modified, +7/-0

**Shared rationale:** install app bridge at the intended wrapper point; normal CLI remains dormant.


### `@@ -1,5 +1,6 @@`

- **ADD** `import eval.app_cross_platform_bridge as app_cross_platform_bridge` — install app bridge at the intended wrapper point; normal CLI remains dormant; exact location: `@@ -1,5 +1,6 @@`.

### `@@ -95,6 +96,12 @@`

- **ADD** `# Desktop Eval window only: keep all benchmark/data/scoring logic above, but let` — install app bridge at the intended wrapper point; normal CLI remains dormant; exact location: `@@ -95,6 +96,12 @@`.
- **ADD** `# the production app backend own inference/training on non-MLX Windows/Linux/Unix.` — install app bridge at the intended wrapper point; normal CLI remains dormant; exact location: `@@ -95,6 +96,12 @@`.
- **ADD** `# The bridge is a no-op for normal CLI runs and for Apple MLX it preserves the` — install app bridge at the intended wrapper point; normal CLI remains dormant; exact location: `@@ -95,6 +96,12 @@`.
- **ADD** `# existing native eval path.` — install app bridge at the intended wrapper point; normal CLI remains dormant; exact location: `@@ -95,6 +96,12 @@`.
- **ADD** `app_cross_platform_bridge.install(master_runtime, phase4_pro_rsi, Master4000EvaluationEngine)` — install app bridge at the intended wrapper point; normal CLI remains dormant; exact location: `@@ -95,6 +96,12 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — install app bridge at the intended wrapper point; normal CLI remains dormant; exact location: `@@ -95,6 +96,12 @@`.

## `pyproject.toml` — modified, +2/-0

**Shared rationale:** declare cross-platform packaged Eval dependencies.


### `@@ -62,6 +62,7 @@ dependencies = [`

- **ADD** `"datasets>=2.18.0",` — declare cross-platform packaged Eval dependencies; exact location: `@@ -62,6 +62,7 @@ dependencies = [`.

### `@@ -74,6 +75,7 @@ dependencies = [`

- **ADD** `"llama-cpp-python>=0.3.0; sys_platform != 'darwin' or platform_machine != 'arm64'",` — declare cross-platform packaged Eval dependencies; exact location: `@@ -74,6 +75,7 @@ dependencies = [`.

## `requirements.txt` — modified, +2/-0

**Shared rationale:** mirror cross-platform Eval dependencies.


### `@@ -9,6 +9,7 @@ pydantic-settings>=2.0.0`

- **ADD** `datasets>=2.18.0` — mirror cross-platform Eval dependencies; exact location: `@@ -9,6 +9,7 @@ pydantic-settings>=2.0.0`.

### `@@ -21,6 +22,7 @@ mlx-vlm; sys_platform == "darwin" and platform_machine == "arm64"`

- **ADD** `llama-cpp-python>=0.3.0; sys_platform != "darwin" or platform_machine != "arm64"` — mirror cross-platform Eval dependencies; exact location: `@@ -21,6 +22,7 @@ mlx-vlm; sys_platform == "darwin" and platform_machine == "arm64"`.

## `run_studio_complete.py` — modified, +1/-1

**Shared rationale:** use requested Bonsai 2 as standalone eval default.


### `@@ -34,7 +34,7 @@ def compute_auto_kv_budget(total_ram_gb: float)->int:`

- **REMOVE** `mlx_model_path: str="penkia/TernaryQuench-Qwen3.8-27B-MLX"` — use requested Bonsai 2 as standalone eval default; exact location: `@@ -34,7 +34,7 @@ def compute_auto_kv_budget(total_ram_gb: float)->int:`.
- **ADD** `mlx_model_path: str="prism-ml/Ternary-Bonsai-2-27B-mlx-2bit"` — use requested Bonsai 2 as standalone eval default; exact location: `@@ -34,7 +34,7 @@ def compute_auto_kv_budget(total_ram_gb: float)->int:`.

## `tests/unit/test_app_eval_integration_contract.py` — added, +104/-0

**Shared rationale:** lock Eval UI/backend/packaging/top-control invariants.


### `@@ -0,0 +1,104 @@`

- **ADD** `"""Source contracts for the additive desktop Eval integration."""` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `from pathlib import Path` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `ROOT = Path(__file__).resolve().parents[2]` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `def _src(path: str) -> str:` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `return (ROOT / path).read_text(encoding="utf-8")` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `def test_eval_button_is_text_only_and_cancel_is_confirmed():` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `src = _src("core/gui_eval_panel.py")` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `assert 'text="Eval"' in src` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `assert 'model_type", "text"' in src` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `assert "btn.pack_forget()" in src` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `assert 'messagebox.askyesno(' in src` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `assert '"Cancel Evaluation"' in src` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `assert "pause.flag" in src` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `assert "CTRL_BREAK_EVENT" in src` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `assert "signal.SIGINT" in src` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `def test_eval_window_has_live_output_and_process_tree_memory_limit():` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `src = _src("core/gui_eval_panel.py")` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `runner = _src("eval/app_eval_runner.py")` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `assert "os.read(fd, 4096)" in src` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `assert "getincrementaldecoder" in src` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `assert "children(recursive=True)" in src` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `assert "memory_limit_gb" in src` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `assert "SmartAI-EvalMemoryGuard" in runner` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `assert "root.children(recursive=True)" in runner` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `assert "memory_limit_enabled" in runner` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `def test_app_eval_reuses_production_backends_and_fails_closed_on_fake_training():` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `bridge = _src("eval/app_cross_platform_bridge.py")` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `assert "ProReasoningEngine" in bridge` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `assert "self.pro.load_model(" in bridge` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `assert "backend.stream_generate_tokens(" in bridge` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `assert "backend.generate_branches(" in bridge` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `assert "training_ready" in bridge` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `assert "train_mini_batch" in bridge` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `assert "zero parameter drift" in bridge` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `assert "no persisted adapter/model artifact" in bridge` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `assert "_backend_context_limit" in bridge` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `assert "_clamp_output_to_backend" in bridge` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `def test_eval_state_is_isolated_but_native_runtime_cache_remains_shared():` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `runner = _src("eval/app_eval_runner.py")` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `bridge = _src("eval/app_cross_platform_bridge.py")` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `gguf = _src("core/engines/gguf_engine.py")` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `bitnet = _src("core/engines/bitnet_cpp_engine.py")` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `controller = _src("core/controller_runtime.py")` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `assert 'os.environ["SMARTAI_DATA_DIR"]' not in runner` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `assert "SMARTAI_GGUF_ADAPTER_ROOT" in runner` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `assert "SMARTAI_BITNET_TRAINING_ROOT" in runner` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `assert "SMARTAI_CONTROLLER_ADAPTER_ROOT" in runner` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `assert 'backend_state", "mlx", "adapters.safetensors' in bridge` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `assert "SMARTAI_GGUF_ADAPTER_ROOT" in gguf` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `assert "SMARTAI_BITNET_TRAINING_ROOT" in bitnet` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `assert "SMARTAI_CONTROLLER_ADAPTER_ROOT" in controller` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `def test_canonical_suite_order_and_standalone_packaging_are_preserved():` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `suite = _src("master_4000_eval_suite.py")` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `build = _src("build_app.py")` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `bridge = suite.index("app_cross_platform_bridge.install(master_runtime")` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `integrity = suite.index("install_stage_integrity_telemetry(phase4_pro_rsi")` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `legacy = suite.index("install_rsi_legacy_training_hardening(phase4_pro_rsi")` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `real_data = suite.index("real_benchmark_runtime.install(BenchmarkDatasetProvider")` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `context = suite.index("unified_context_budget.install(")` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `assert bridge < integrity < legacy < real_data < context` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `assert build.count('"eval"') >= 3` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `assert '"master_4000_eval_suite.py"' in build` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `assert '"run_studio_complete.py"' in build` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `def test_bonsai2_is_the_default_eval_model_and_current_model_is_reported():` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `runtime = _src("run_studio_complete.py")` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `report = _src("eval/_master_4000_base.py")` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `assert 'mlx_model_path: str="prism-ml/Ternary-Bonsai-2-27B-mlx-2bit"' in runtime` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `assert "_eval_target_model_label" in report` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `assert "Bonsai 2 27B Ternary Multimodal" in report` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `def test_top_app_controls_explicitly_include_context_limit_and_memory_watcher():` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `app = _src("app_gui.py")` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `context = _src("core/gui_generation_cap.py")` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `eval_ui = _src("core/gui_eval_panel.py")` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `# One existing app Context control + one existing top-app memory control.` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `# Eval gets its own memory watcher but must not clone the normal app controls.` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `assert app.count("install_gui_generation_cap()") == 1` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `assert context.count('text="Context Limit"') == 1` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `assert app.count('text="Mem Limit"') == 1` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `assert 'text="Context Limit"' not in eval_ui` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `assert 'text="Mem Limit"' not in eval_ui` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `assert "self.memory_limit_var = tk.BooleanVar" in app` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `assert "self.entry_memory_limit.pack(" in app` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `assert 'text="GB"' in app` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.
- **ADD** `assert "tk.Toplevel(self.root)" in eval_ui` — lock Eval UI/backend/packaging/top-control invariants; exact location: `@@ -0,0 +1,104 @@`.

## `tests/unit/test_diverged_branch_reconciliation_contract.py` — added, +85/-0

**Shared rationale:** lock best-of-old reconciliation and reject stale regressions.


### `@@ -0,0 +1,85 @@`

- **ADD** `"""Contracts for behavior reconciled from old diverged branches.` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `These tests intentionally lock only the useful pieces we preserved. They also lock` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `the newer policies that must NOT regress (question-free RSI memory, full-precision` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `KV, and disabled speculative decoding until equivalence is proven).` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `"""` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `from pathlib import Path` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `ROOT = Path(__file__).resolve().parents[2]` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `def _src(path: str) -> str:` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `return (ROOT / path).read_text(encoding="utf-8")` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `def _section(source: str, start: str, end: str) -> str:` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `a = source.index(start)` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `b = source.index(end, a)` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `return source[a:b]` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `def test_rsi_round2_keeps_answer_blind_feedback_without_old_persistence():` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `phase = _src("eval/phase4_pro_rsi.py")` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `rsi = _section(` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `phase,` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `"def _run_rsi_self_improvement",` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `"def _fetch_benchmark_training_memories",` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `)` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `assert "Answer-blind deterministic verifier feedback from that attempt" in rsi` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `assert "_answer_blind_failure_feedback(" in rsi` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `assert '_assert_same_model(self, model_identity, f"RSI round {round_idx}")' in rsi` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `assert "branches.clear()" in rsi` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `assert "log_rsi_self_memory(candidate)" in rsi` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `assert "log_interaction(" not in rsi` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `def test_rsi_memory_keeps_newer_full_precision_policy_plus_safe_sync():` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `source = _src("eval/rsi_generation_memory_hardening.py")` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `cleanup = _section(source, "def _clear_runtime_memory", "def _memory_policy")` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `assert 'sync = getattr(mx, "synchronize", None)' in cleanup` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `assert "if callable(sync):" in cleanup` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `assert "mx.clear_cache()" in cleanup` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `assert "KV=full" in source` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `assert "full-precision KV" in source` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `# Old branch's lossy/quantized fallback must stay retired.` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `assert "kv_bits=" not in source` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `assert "quantized_kv_start=" not in source` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `def test_recovered_drafter_is_diagnostic_only_and_does_not_enable_speculation():` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `drafter = _src("core/drafter.py")` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `speculative = _src("core/speculative_engine.py")` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `assert "class DraftTelemetry" in drafter` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `assert "class GrammarGuidedASTTrieDrafter" in drafter` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `assert "ASTPrefixTrieDrafter = GrammarGuidedASTTrieDrafter" in drafter` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `assert "def get_telemetry" in drafter` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `assert "Speculative decoding is disabled" in speculative` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `assert "def propose_draft_tokens" in speculative` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `assert "return []" in speculative` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `def test_recovered_lif_helpers_do_not_replace_current_runtime_policy():` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `lif = _src("core/lif_gating.py")` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `pro = _src("core/pro_engine.py")` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `assert "def convex_temperature_ladder" in lif` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `assert "def normalized_shannon_from_probabilities" in lif` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `assert "def topk_shannon_from_logits" in lif` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `assert "def spike_count" in lif` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `# Current calibrated Pro policy remains owned by pro_engine/temperature policy.` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `assert "def get_ladder_temperatures" in pro` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `assert "gamma: float = 1.35" in pro` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `def test_projected_daemon_gains_lifecycle_telemetry_without_old_architecture_rewire():` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `daemon = _src("consolidation/projected_daemon.py")` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `pro = _src("core/pro_engine.py")` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `assert "def stop(self)" in daemon` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `assert "def status(self)" in daemon` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `assert "self.last_error" in daemon` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `assert "self.last_update_time" in daemon` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `assert "gc.collect(2)" in daemon` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `assert "AwakeOnlineConsolidator" in pro` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `# Do not revive the old MLX-only wrapper architecture.` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `assert "_LIFRouterAdapter" not in pro` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.
- **ADD** `assert "_MemoryReplayBridge" not in pro` — lock best-of-old reconciliation and reject stale regressions; exact location: `@@ -0,0 +1,85 @@`.

## `tests/unit/test_full_branch_audit_contract.py` — added, +181/-0

**Shared rationale:** lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring.


### `@@ -0,0 +1,181 @@`

- **ADD** `"""Full-branch audit contracts.` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `Locks concrete regressions found while auditing fix/real-benchmarks-final-32k` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `against master and the historical Gemini handoff. These are source contracts, not` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `claims that heavyweight native backends were executed on every target machine.` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `"""` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `from pathlib import Path` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `ROOT = Path(__file__).resolve().parents[2]` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `def _src(path: str) -> str:` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `return (ROOT / path).read_text(encoding="utf-8")` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `def test_eval_has_no_fabricated_speed_or_speculative_fallbacks():` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `base = _src("eval/_master_4000_base.py")` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `runtime = _src("eval/master_4000_runtime.py")` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert "self.last_tok_per_sec = 12.0" not in base` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert "getattr(self, 'last_tok_per_sec', 15.0)" not in base` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert "spec_rate=42.5" not in base` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert "[Offline:" not in base` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert "refusing synthetic/offline benchmark output" in base` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert "process_memory_bytes()" in runtime` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `def test_cross_platform_eval_imports_do_not_require_mlx_at_module_import():` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `live = _src("eval/live_generation_stream.py")` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `rsi = _src("eval/rsi_generation_memory_hardening.py")` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert "try:\n    from mlx_lm.models.cache import make_prompt_cache" in live` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert "make_prompt_cache = None" in live` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert "try:\n    from mlx_lm.models.cache import make_prompt_cache" in rsi` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert "make_prompt_cache = None" in rsi` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `def test_app_eval_adapter_satisfies_legacy_engine_surface_and_gguf_teach_identity():` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `bridge = _src("eval/app_cross_platform_bridge.py")` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `teach = _src("eval/conversation_teach_hardening.py")` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert "NeuromorphicLIFController" in bridge` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert "self.lif = NeuromorphicLIFController()" in bridge` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert 'if getattr(backend, "model", None) is not self.engine.model:' in teach` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert 'if getattr(backend, "tokenizer", None) is not self.engine.tokenizer:' in teach` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `def test_one_context_budget_controls_generation_for_every_text_backend():` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `awake = _src("core/awake_auto_hook.py")` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert "def _remaining_generation_tokens" in awake` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert "Packed prompt requires" in awake` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert 'previous_max = getattr(self.settings, "max_new_tokens", None)' in awake` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert "self.settings.max_new_tokens = int(remaining)" in awake` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert "self.settings.max_new_tokens = previous_max" in awake` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `def test_eval_memory_limit_uses_mac_physical_footprint_when_available():` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `memory = _src("core/training_memory.py")` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `gui = _src("core/gui_eval_panel.py")` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `runner = _src("eval/app_eval_runner.py")` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert "def process_memory_bytes" in memory` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert "ri_phys_footprint" in memory` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert "process_memory_bytes(item.pid)" in gui` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert "process_memory_bytes(proc.pid)" in runner` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `def test_mlx_lora_is_adapter_only_and_transactional():` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `src = _src("core/_mlx_engine_base.py")` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert src.count('unfreeze(keys=["lora_a", "lora_b"], recurse=False)') >= 4` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert "MLX training completed but measured zero parameter change" in src` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert "rollback_params" in src` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert ".next.safetensors" in src` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert ".previous" in src` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert "self.model.update(" in src` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `def test_non_mlx_training_and_cancel_paths_roll_back():` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `gguf = _src("core/engines/gguf_engine.py")` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `bitnet = _src("core/engines/bitnet_cpp_engine.py")` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `controller = _src("core/controller_runtime.py")` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `gguf_trainer = _src("core/gguf_lora_trainer.py")` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `bitnet_trainer = _src("core/bitnet_rebuild_trainer.py")` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `bridge = _src("eval/app_cross_platform_bridge.py")` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert "except BaseException:" in gguf` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert "except BaseException:" in bitnet` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert "except BaseException:" in gguf_trainer` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert "except BaseException:" in bitnet_trainer` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert "before = {" in controller` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert "model.save_pretrained(tmp" in controller` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert "with torch.no_grad():" in controller` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `persisted = bridge.index("persisted = bool(adapter_path")` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `transaction = bridge.index("with sqlite3.connect(db_path) as conn:")` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `learn_update = bridge.index("UPDATE episodic_interactions SET consolidated=1")` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `rsi_update = bridge.index("UPDATE rsi_self_memories SET consolidated=1")` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert persisted < transaction < learn_update < rsi_update` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `def test_media_learning_never_equates_file_existence_with_real_learning():` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `media = _src("core/media_learning.py")` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert "def _persisted_lora_delta_proven" in media` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert "LoRA update factor could not be proven" in media` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert "delta_sq <= 0" in media` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert "except BaseException:" in media` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `def test_swe_patch_verification_is_portable():` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `src = _src("eval/swe_verifier_hardening.py")` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert 'platform.system() == "Windows"' in src` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert 'shutil.which("patch")' in src` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert 'shutil.which("git")' in src` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert '"apply", "--check"' in src` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `def test_report_target_model_source_is_valid_python_not_literal_escape():` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `src = _src("eval/_master_4000_base.py")` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert '_eval_target_model_label' in src` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert '))\\n        md.append' not in src` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `def test_cross_platform_eval_uses_native_token_counting_before_fallback():` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `bridge = _src("eval/app_cross_platform_bridge.py")` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert 'tokenize = getattr(tokenizer, "tokenize", None)' in bridge` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert 'counter = getattr(backend, "count_tokens", None)' in bridge` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert 'backend=getattr(self.engine, "backend", None)' in bridge` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `def test_non_mlx_phase3_marks_learn_and_rsi_atomically():` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `bridge = _src("eval/app_cross_platform_bridge.py")` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `persisted = bridge.index("persisted = bool(adapter_path")` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `transaction = bridge.index("with sqlite3.connect(db_path) as conn:")` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `learn_update = bridge.index("UPDATE episodic_interactions SET consolidated=1")` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `rsi_update = bridge.index("UPDATE rsi_self_memories SET consolidated=1")` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert persisted < transaction < learn_update < rsi_update` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `def test_rollback_backups_survive_until_successful_return():` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `controller = _src("core/controller_runtime.py")` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `gguf = _src("core/engines/gguf_engine.py")` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `bitnet = _src("core/engines/bitnet_cpp_engine.py")` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `for source in (controller, gguf, bitnet):` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert "success = False" in source` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert "success = True" in source` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert "except BaseException:" in source` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert "finally:" in source` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `def test_eval_memory_guard_interrupts_main_thread_portably():` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `runner = _src("eval/app_eval_runner.py")` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert "import _thread" in runner` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert "_thread.interrupt_main()" in runner` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `def test_rsi_resume_preserves_question_free_self_memory_only():` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `resume = _src("eval/rsi_resume_hardening.py")` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert "SELECT trace FROM rsi_self_memories" in resume` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert "log_rsi_self_memory(candidate)" in resume` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert "clear_unconsolidated_rsi_self_memories = guarded_clear_self" in resume` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert "log_interaction(" not in resume` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `def test_end_to_end_call_chain_is_wired_through_existing_components():` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `app = _src("app_gui.py")` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `media = _src("core/media_orchestrator.py")` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `init = _src("core/__init__.py")` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `learner = _src("core/autonomous_learner.py")` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `suite = _src("master_4000_eval_suite.py")` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `phase = _src("eval/phase4_pro_rsi.py")` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `blank-line formatting change within this audited hunk` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert "self.multimodal.stream_solve(full_msg" in app` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert "self.learner.run_learning_session(" in app` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert "iterator = self.app.engine.stream_solve(" in media` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert "self.learning.learn" in media` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert init.index("install_awake_auto_learning") < init.index("install_full_context_hardening")` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert "def run_learning_session" in learner` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert "self.consolidate_parameters(" in learner` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert "self.recursive_self_improve(" in learner` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert "self.consolidate_rsi_parameters(" in learner` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert "app_cross_platform_bridge.install(" in suite` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert "▶ PHASE 1: ZERO-SHOT SINGLE-PASS BASELINE" in phase` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert "▶ PHASE 2: SUPPLIED LEARN / MEMORY INGESTION + MCTS TEACHING" in phase` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert "▶ RSI: RECURSIVE SELF-IMPROVEMENT ON PHASE-1 MISSES" in phase` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert "▶ PHASE 3: LEARN + RSI PARAMETRIC CONSOLIDATION" in phase` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
- **ADD** `assert "Phase 4: Post-Consolidation" in phase` — lock full-audit defects, rollback, portability, RSI resume and end-to-end wiring; exact location: `@@ -0,0 +1,181 @@`.
