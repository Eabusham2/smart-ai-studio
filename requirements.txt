torch>=2.2.0
transformers>=4.40.0
accelerate>=0.28.0
peft>=0.10.0
sympy>=1.12
pydantic>=2.0.0
pydantic-settings>=2.0.0
rich>=13.0.0
pytest>=7.0.0

# Apple Silicon (macOS) MLX native acceleration (Optional)
# mlx>=0.16.0; sys_platform == 'darwin' and platform_machine == 'arm64'
# mlx-lm>=0.18.0; sys_platform == 'darwin' and platform_machine == 'arm64'

